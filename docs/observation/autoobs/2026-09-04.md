# /autoobs — 2026-09-04

**Sweep time:** 17:04 UTC
**Overall:** DEGRADED — every non-synthetic monitor and SLO reads `No Data`. This is
expected for a first-ever sweep (RUM only just started sending in this feature branch,
per `research.md` finding 1: "RUM has almost certainly never initialized in
production"), not a regression — but it means the RUM/API-health signals below cannot
yet be verified as HEALTHY, only as "correctly wired and waiting for traffic."

## Monitors

12 monitors tagged `service:yogakit` (8 manifest-managed + 4 synthetic-companion
drift, matching `datadog/README.md` "Expected drift"):

| Monitor | State | Source |
|---|---|---|
| Server Error Rate > 5% | No Data | manifest |
| Server Request Latency p95 > 5s | No Data | manifest |
| RUM Error Rate > 5% | No Data | manifest |
| Synthetic: Read View Unreachable | No Data | manifest |
| Interaction to Next Paint p75 > 500ms | No Data | manifest |
| Largest Contentful Paint p75 > 2.5s | No Data | manifest |
| SLO Burn (breached) — Read View Availability | No Data | manifest |
| SLO Burn (trending) — Read View Availability | No Data | manifest |
| Synthetic: Homepage Returns 200 | **OK** | drift (expected) |
| Synthetic: Poses Index Returns 200 | **OK** | drift (expected) |
| Synthetic: Read View Returns 200 | **OK** | drift (expected) |
| Synthetic Browser: Read Flow Offline | No Data | drift (expected — `status: paused`) |

`npm run datadog:diff --type monitors` also confirms all 8 manifest monitors read
`no-change | in sync` against `datadog/monitors/*.json`.

## SLOs

`npm run datadog:diff --type slos`: both `no-change | in sync`.

| SLO | Target | SLI (30d) |
|---|---|---|
| Read View Availability | 99.5% | n/a — no history yet |
| RUM Error-Free Sessions | 99% | n/a — no history yet |

## Core Web Vitals & RUM

`@application.id:91af99b0-865b-405f-914e-bda170dc43b7` over the last 24h: **0 RUM
sessions.** Confirmed the same under an unscoped `service:yogakit` query (also 0) —
per the command's own tagging-rule check, 0-in-both means genuinely no browser
traffic yet, not a RUM scoping bug. No production/preview traffic has hit an
RUM-instrumented page since the application was created this branch.

## API error rate & latency

No data — consistent with the monitors above; no server request volume tagged
`service:yogakit env:prod` in the last 24h yet in this org.

## Synthetic uptime

| Test | Type | Status | Last 24h |
|---|---|---|---|
| Homepage Returns 200 | api | live | 10/10 passed |
| Poses Index Returns 200 | api | live | 10/10 passed |
| Read View Returns 200 | api | live | 10/10 passed |
| Read Flow Offline | browser | paused (expected, per plan) | n/a |

All 3 live API synthetics are the only signal source with real production traffic
right now, and all 3 are passing 100% over the last 24h.

## Dashboard

`[YogaKit] Health` (id `fpi-4fq-265`) reachable via `GET /api/v1/dashboard/<id>`, 3
widgets present.

## Findings

1. **No RUM/API traffic yet (expected, not a bug).** This is the first sweep after
   RUM went live in us5 for the first time (this branch). Every monitor/SLO tied to
   RUM or API request volume correctly reads `No Data` rather than a false `OK` or an
   error — the wiring is correct, there is simply nothing to observe yet. Re-run this
   sweep once the branch merges and picks up real traffic to get the first genuine
   HEALTHY/AT-RISK read on these signals.
2. No unexpected drift, no manifest validation failures, no monitor/SLO
   misconfiguration, dashboard reachable. Synthetic uptime is the one signal with real
   data this sweep, and it is 100% passing.
