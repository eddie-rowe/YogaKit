# /autoobs — 2026-09-25

**Sweep time:** 08:05 UTC
**Overall:** HEALTHY

## Monitors

16 monitors tagged `service:yogakit` (11 matching `datadog/monitors/*.json` manifests + 5 expected drift — Datadog's auto-created synthetics companion monitors, per `datadog/README.md` "Expected drift"). 9 OK / 2 Alert / 5 No Data.

| Monitor | Status | Manifest |
|---|---|---|
| `[YogaKit] Server Error Rate > 5%` | **OK** (recovered) | `api-error-rate.json` — was No Data every sweep through 2026-09-24, fixed by `#64` (`b7a763e`, merged 2026-09-24 PM, after yesterday's sweep) |
| `[YogaKit] Server Request Latency p95 > 5s` | OK | `api-latency-p95.json` |
| `[YogaKit] RUM Error Rate > 5%` | No Data | `rum-error-rate.json` — 0 real-user RUM sessions, expected pre-launch |
| `[YogaKit] Synthetic Test Runs Missing` | OK | `synthetic-read-view-down.json` |
| `[YogaKit] Real-user Page Load p75 > 4s` | No Data | `web-vitals-inp.json` (legacy filename) — 0 real-user sessions |
| `[YogaKit] Largest Contentful Paint p75 > 2.5s` | No Data | `web-vitals-lcp.json` — 0 real-user sessions |
| `[YogaKit] Production Logs Missing` | OK | `log-ingestion-liveness.json` |
| `[YogaKit] Read View Error Budget Consumed > 25%` (fast burn) | **Alert** | `slo-burn-fast-read-view-availability.json` — see Findings #1, known/expected |
| `[YogaKit] Read View Error Budget Consumed > 10%` (slow burn) | **Alert** | `slo-burn-slow-read-view-availability.json` — see Findings #1, known/expected |
| `[YogaKit] APM Telemetry Missing for 30 Minutes` | **OK** (recovered) | `apm-telemetry-freshness.json` — was No Data every sweep through 2026-09-24, fixed by `#64` |
| `[YogaKit] RUM Telemetry Missing for 30 Minutes` | No Data | `rum-telemetry-freshness.json` — known gap since 2026-09-10, unrelated to `#64` |
| `[YogaKit] Synthetic: Homepage Returns 200` | OK | expected drift (companion monitor) — see Findings #3, live assertion threshold disagrees with manifest |
| `[YogaKit] Synthetic: Poses Index Returns 200` | OK | expected drift (companion monitor) |
| `[YogaKit] Synthetic: Read View Returns 200` | OK | expected drift (companion monitor) |
| `[YogaKit] Synthetic Browser: Read Flow Offline` | No Data (paused, expected) | expected drift (companion monitor) |
| `[YogaKit] Synthetic Browser: Read View RUM Session` | OK | expected drift (companion monitor) |

No unexpected drift among monitor *states* (see "Config drift" below for a monitor-*object*-drift classifier bug found this sweep — different from monitor evaluated status).

## SLOs

| SLO | Current SLI | Target (warn) | State | Error budget remaining (30d) |
|---|---|---|---|---|
| `[YogaKit] Read View Availability` (`read-view-availability`) | 16.912% | 99.5% (warn 99.9%) | **breached** | -16517.6% — see Findings #1, known/expected, do not re-open #39 |
| `[YogaKit] RUM Error-Free Sessions` (`rum-error-free-sessions`) | 100% | 99% (warn 99.5%) | ok | 100% (effectively no real-user session volume) |

## Core Web Vitals & RUM

- Real-user (non-synthetic) RUM traffic, last 24h: **0**. `@application.id:91af99b0-865b-405f-914e-bda170dc43b7`-scoped and unscoped (`service:yogakit env:prod`) RUM event aggregation both return the identical breakdown — **2,869 events, 100% `@session.type:synthetics` + `@browser.name:Chrome`** (the `read-view-rum-session` browser synthetic firing every ~15 min). Connector up, data genuinely absent — expected pre-launch state, unchanged from every prior sweep.
- LCP p75, real-user page-load p75, RUM session-error rate: all null series (no real-user denominator).
- Call: **NO-DATA** for real-user Core Web Vitals and RUM error rate (connector up, traffic genuinely absent, expected pre-launch, unchanged).

## API error rate & latency

Direct query against `trace.web.request*` (the family `api-error-rate.json`/`api-latency-p95.json`/`apm-telemetry-freshness.json` now query, per `#64`), last 24h, `service:yogakit env:prod`:

- Total requests: **1,751**, 5xx count: **0** (null series, i.e. never recorded — consistent with `datadog/README.md`'s note that counters emit no points at count zero) → server error rate **0%** (threshold: critical >5%, warn >2%) → **HEALTHY**
- p95 request duration: **≈42ms** (threshold: critical >5s, warn >2s) → **HEALTHY**
- All three monitors that query this family (`api-error-rate`, `api-latency-p95`, `apm-telemetry-freshness`) now correctly read **OK** rather than No Data — `#64`'s fix (merged after yesterday's sweep) is confirmed live.

## Synthetic uptime

| Test | Last result | 24h | Notes |
|---|---|---|---|
| `homepage-200` (`tgu-u39-72k`) | passed (currently OK) | **1 failure**: `responseTime` 2035.9ms vs live assertion `<2000ms`, `aws:us-west-2`, auto-retried and recovered | see Findings #3 — live assertion is 2000ms, manifest declares 3000ms |
| `poses-index-200` (`rq9-rxb-edd`) | passed | 0 failures / 24h | healthy |
| `read-view-200` (`z76-7mk-der`) | passed | 0 failures / 24h | healthy |
| `read-flow-offline` (browser, `qsv-emk-wqy`) | — | — | `status: paused` (stub, expected — not a failure) |

## Dashboard

`[YogaKit] Health` (`fpi-4fq-265`) — **reachable** via `GET /api/v1/dashboard/fpi-4fq-265` (title + description resolved).

## Config drift

`npm run datadog:drift-check` exited **1**, reporting all 5 documented companion-monitor entries as "Unexpected drift":

```
Unexpected drift (5):
  - monitors: [YogaKit] Synthetic: Homepage Returns 200
  - monitors: [YogaKit] Synthetic: Poses Index Returns 200
  - monitors: [YogaKit] Synthetic: Read View Returns 200
  - monitors: [YogaKit] Synthetic Browser: Read Flow Offline
  - monitors: [YogaKit] Synthetic Browser: Read View RUM Session
```

This is a **false positive** — see Findings #2: a classifier bug, not real drift. Cross-checked against `datadog/README.md`'s own "Expected drift" list and live `pup monitors list` output: all 5 are exactly the documented, permanent synthetics companion-monitor drift.

Separately, `npm run datadog:diff` also reports one real, non-monitor drift row:

```
synthetics-api | [YogaKit] Homepage Returns 200 | update | would apply | config
```

This one is genuine — see Findings #3.

## Findings

1. **SLO breach is the known historical outage continuing to age through the 30-day lookback — not a new incident, do not re-open #39.** `read-view-availability` SLI is 16.912% today (15.096% yesterday), error budget -16517.6%; both burn-rate monitors remain Alert. Unchanged narrative from 2026-09-24: the corrected SLO query (fixed 2026-09-23) now honestly reflects the 2026-09-09–22 outage inside its 30-day window and will self-clear as it ages out (~2026-10-22). Current read-view availability is healthy — `read-view-200` synthetic passed all runs in the last 24h, 0 failures.
2. **NEW — `findUnexpectedDrift()` classifier bug, introduced by `#77`/`a859975` (merged 2026-09-24 14:52 -0500, after yesterday's sweep — this is the first sweep to exercise it against live data).** `scripts/lib/datadog-sync.mjs`'s `EXPECTED_DRIFT_MONITOR_NAMES` list (around line 258) is missing the `"Synthetic: "` / `"Synthetic Browser: "` infix that the live monitor names (and `datadog/README.md`'s own "Expected drift" section) actually carry — e.g. it lists `'[YogaKit] Homepage Returns 200'` but the live/documented name is `'[YogaKit] Synthetic: Homepage Returns 200'`. None of the 5 entries match, so `npm run datadog:drift-check` (`.github/workflows/datadog-drift.yml`, weekly) will spuriously exit 1 on every run until this is fixed. Confirmed a false positive by cross-referencing README's documented list and this sweep's own `pup monitors list` output — record only, not fixed here (out of this command's scope; next `/autodev` or manual fix should update the five string literals).
3. **NEW — genuine (non-classifier) config drift: `homepage-200`'s live `responseTime` assertion (2000ms) disagrees with the manifest's declared value (3000ms).** The manifest (`datadog/synthetics/api/homepage-200.json`) says `< 3000ms`; the live Datadog test (`modified_at: 2026-09-09T20:24:45Z`, never re-synced since) actually enforces `< 2000ms`. This is exactly why the test logged its one 24h failure today (2035.9ms > 2000ms, auto-retried, self-recovered) — that failure would not have tripped the manifest's intended 3000ms. `npm run datadog:apply` would currently loosen the live threshold back to 3000ms since the manifest is source of truth; until someone decides the intended value and syncs it, live and repo stay in disagreement. Record only.
4. No real-user RUM/CWV traffic (0 events beyond the `read-view-rum-session` synthetic), expected pre-launch, re-confirmed via the scoped/unscoped cross-check.
5. Everything else healthy: API error rate 0% / p95 ≈42ms (both monitors now reading OK per `#64`), poses-index + read-view synthetics 100% passing, dashboard reachable.
