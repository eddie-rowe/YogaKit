# /autoobs — 2026-09-23

**Sweep time:** 08:21 UTC
**Overall:** HEALTHY

## Monitors

16 monitors tagged `service:yogakit` (11 matching `datadog/monitors/*.json` manifests + 5 expected drift — Datadog's auto-created synthetics companion monitors, per `datadog/README.md` "Expected drift"). 7 OK / 1 Alert / 8 No Data.

| Monitor | Status | Manifest |
|---|---|---|
| `[YogaKit] Server Error Rate > 5%` | No Data | `api-error-rate.json` — see Findings #3, live traffic confirmed healthy via direct span query |
| `[YogaKit] Server Request Latency p95 > 5s` | No Data | `api-latency-p95.json` — see Findings #3 |
| `[YogaKit] RUM Error Rate > 5%` | No Data | `rum-error-rate.json` — 0 real-user RUM sessions, expected pre-launch |
| `[YogaKit] Synthetic Test Runs Missing` | OK | `synthetic-read-view-down.json` |
| `[YogaKit] Real-user Page Load p75 > 4s` | No Data | `web-vitals-inp.json` (legacy filename) — 0 real-user sessions |
| `[YogaKit] Largest Contentful Paint p75 > 2.5s` | No Data | `web-vitals-lcp.json` — 0 real-user sessions |
| `[YogaKit] Production Logs Missing` | OK | `log-ingestion-liveness.json` |
| `[YogaKit] Read View Error Budget Consumed > 25%` (fast burn) | OK | `slo-burn-fast-read-view-availability.json` |
| `[YogaKit] Read View Error Budget Consumed > 10%` (slow burn) | **Alert** | `slo-burn-slow-read-view-availability.json` — see Findings #2, expected residual |
| `[YogaKit] APM Telemetry Missing for 30 Minutes` | No Data | `apm-telemetry-freshness.json` — see Findings #3 |
| `[YogaKit] RUM Telemetry Missing for 30 Minutes` | No Data | `rum-telemetry-freshness.json` |
| `[YogaKit] Synthetic: Homepage Returns 200` | OK | expected drift (companion monitor) |
| `[YogaKit] Synthetic: Poses Index Returns 200` | OK | expected drift (companion monitor) |
| `[YogaKit] Synthetic: Read View Returns 200` | **OK** (recovered) | expected drift (companion monitor) — was Alert every prior sweep since 2026-09-21, see Findings #1 |
| `[YogaKit] Synthetic Browser: Read Flow Offline` | No Data (paused, expected) | expected drift (companion monitor) |
| `[YogaKit] Synthetic Browser: Read View RUM Session` | OK | expected drift (companion monitor) |

No unexpected drift observed.

## SLOs

| SLO | Current SLI | Target (warn) | State | Error budget remaining (30d) |
|---|---|---|---|---|
| `[YogaKit] Read View Availability` (`read-view-availability`) | 99.902% | 99.5% (warn 99.9%) | **ok** (recovered from `warning`) | 80.4% (up from 79.64% on 2026-09-22 08:05) |
| `[YogaKit] RUM Error-Free Sessions` (`rum-error-free-sessions`) | 100% | 99% (warn 99.5%) | ok | 100% (effectively no real-user session volume) |

## Core Web Vitals & RUM

- Real-user (non-synthetic) RUM sessions, last 24h: **0**, confirmed both scoped (`@application.id:91af99b0-865b-405f-914e-bda170dc43b7`, `NEXT_PUBLIC_DD_RUM_APPLICATION_ID`) and unscoped (`service:yogakit env:prod`) — both read 96 unique sessions, all `@session.type:synthetics` (the `read-view-rum-session` browser synthetic firing every 15 min, 96 × 15min = 24h). Connector up, data genuinely absent — expected pre-launch state, unchanged from every prior sweep.
- LCP p75 (synthetic-only traffic, informational): ≈378ms. Real-user page-load-time p75 (synthetic-only traffic): ≈1.33s. Both well under threshold but reflect the synthetic prober, not real users.
- RUM error count, last 24h: **0**.
- Call: **NO-DATA** for real-user Core Web Vitals and RUM error rate (connector up, traffic genuinely absent, expected pre-launch, unchanged).

## API error rate & latency

Datadog's derived-metric monitors (`api-error-rate.json`, `api-latency-p95.json`) currently read **No Data** (see Findings #3). Cross-checked directly against raw APM spans, last 24h, `service:yogakit env:prod operation_name:web.request`:

- Total requests: **2,031**, all `http.status_code:200` → server error rate **0%** (threshold: critical >5%, warn >2%) → **HEALTHY**
- p95 request duration: **≈77.9ms** (threshold: critical >5s, warn >2s) → **HEALTHY**
- Live span data confirmed as recent as 2026-09-23T08:14:49Z on current production deployment `dpl_EBtx2FF7cUEdCphz4ib8w3piDy8j`, commit `489bfdc`.

## Synthetic uptime

| Test | Last result | 24h | Notes |
|---|---|---|---|
| `homepage-200` (`tgu-u39-72k`) | passed | sampled runs passing, both locations | healthy |
| `poses-index-200` (`rq9-rxb-edd`) | passed | sampled runs passing, both locations | healthy |
| `read-view-200` (`z76-7mk-der`) | **passed** | last failure 2026-09-22T15:34:56Z (~16.75h before this sweep); all sampled runs since then passing, both locations (`aws:us-east-1`, `aws:us-west-2`) | **Recovered** — see Findings #1 |
| `read-flow-offline` (browser, `qsv-emk-wqy`) | — | — | `status: paused` (stub, expected — not a failure) |

## Dashboard

`[YogaKit] Health` (`fpi-4fq-265`) — **reachable** via `GET /api/v1/dashboard/fpi-4fq-265`.

## Findings

1. **RECOVERED — read view synthetic back to passing, SLO back to `ok`.** `read-view-200` (`z76-7mk-der`) last failed at 2026-09-22T15:34:56Z; every sampled run since (spanning several hours up to this sweep, both probe locations) has passed with the expected `"60-Minute Yin: Classic Full Body"` body content. `[YogaKit] Synthetic: Read View Returns 200` monitor is now OK (was Alert every sweep since 2026-09-21). SLO `read-view-availability` is back to `ok` state (SLI 99.902%, error budget remaining 80.4%, up from 79.64% on 2026-09-22 08:05). This tracks with PR #54 (issue #52, `getBuiltInFlowById` UUID-keying fix) landing and a subsequent production deployment (`dpl_EBtx2FF7cUEdCphz4ib8w3piDy8j`, commit `489bfdc`, confirmed live via APM spans as recent as 08:14:49Z today). Open issue [#39](https://github.com/eddie-rowe/YogaKit/issues/39) (`auto/observation`) tracks the now-resolved outage — this sweep does not close it (record-only scope; overall verdict is HEALTHY, not AT-RISK, so no escalation action taken either way), but it appears safe to close on operator or next-sweep confirmation.
2. **Expected residual — slow-burn SLO monitor still Alert.** `[YogaKit] Read View Error Budget Consumed > 10%` evaluates cumulative 30-day budget consumption (query: `error_budget(...).over("30d") > 10`), currently ~19.6% consumed — still above its 10% threshold from the 2026-09-21–22 outage. This will self-clear as that failure window ages out of the 30-day lookback; not a new or active issue.
3. **Monitor/derived-metric discrepancy, not a real gap.** `api-error-rate`, `api-latency-p95`, and `apm-telemetry-freshness` monitors all read **No Data** in Datadog this sweep (a change from 2026-09-22, when `api-error-rate`/`api-latency-p95` were OK). Direct query of raw APM spans (not the derived `trace.next_js.*` metrics the monitors use) confirms traffic is live and healthy: 2,031 requests/24h, 0% errors, p95 ≈78ms, most recent span at 08:14:49Z today. Likely an artifact of the derived-metric pipeline on sparse, synthetic-only traffic (e.g. the 5xx-count metric series may not exist at all when zero 5xx responses have ever been recorded, reading as No Data rather than 0). Connector up, underlying signal healthy — worth a follow-up look at the monitor queries' evaluation windows, but not blocking.
4. No real-user RUM traffic (0 sessions, last 24h) — expected pre-launch state, unchanged, re-confirmed via the scoped/unscoped cross-check.
5. Everything else healthy: homepage + poses-index synthetics 100% passing (sampled runs, both locations), dashboard reachable, no unexpected monitor drift.
