# /autoobs — 2026-09-26

**Sweep time:** 08:06 UTC
**Overall:** HEALTHY

## Monitors

16 monitors tagged `service:yogakit` (11 matching `datadog/monitors/*.json` manifests + 5 expected drift — Datadog's auto-created synthetics companion monitors, per `datadog/README.md` "Expected drift"). 9 OK / 2 Alert / 5 No Data — unchanged distribution from 2026-09-25.

| Monitor | Status | Manifest |
|---|---|---|
| `[YogaKit] Server Error Rate > 5%` | OK | `api-error-rate.json` |
| `[YogaKit] Server Request Latency p95 > 5s` | OK | `api-latency-p95.json` |
| `[YogaKit] RUM Error Rate > 5%` | No Data | `rum-error-rate.json` — 0 real-user RUM sessions, expected pre-launch |
| `[YogaKit] Synthetic Test Runs Missing` | OK | `synthetic-read-view-down.json` |
| `[YogaKit] Real-user Page Load p75 > 4s` | No Data | `web-vitals-inp.json` (legacy filename) — 0 real-user sessions |
| `[YogaKit] Largest Contentful Paint p75 > 2.5s` | No Data | `web-vitals-lcp.json` — 0 real-user sessions |
| `[YogaKit] Production Logs Missing` | OK | `log-ingestion-liveness.json` |
| `[YogaKit] Read View Error Budget Consumed > 25%` (fast burn) | **Alert** | `slo-burn-fast-read-view-availability.json` — see Findings #1, known/expected |
| `[YogaKit] Read View Error Budget Consumed > 10%` (slow burn) | **Alert** | `slo-burn-slow-read-view-availability.json` — see Findings #1, known/expected |
| `[YogaKit] APM Telemetry Missing for 30 Minutes` | OK | `apm-telemetry-freshness.json` |
| `[YogaKit] RUM Telemetry Missing for 30 Minutes` | No Data | `rum-telemetry-freshness.json` — known gap since 2026-09-10, unrelated to `#64` |
| `[YogaKit] Synthetic: Homepage Returns 200` | OK | expected drift (companion monitor) — see Findings #2, live assertion threshold still disagrees with manifest |
| `[YogaKit] Synthetic: Poses Index Returns 200` | OK | expected drift (companion monitor) |
| `[YogaKit] Synthetic: Read View Returns 200` | OK | expected drift (companion monitor) |
| `[YogaKit] Synthetic Browser: Read Flow Offline` | No Data (paused, expected) | expected drift (companion monitor) |
| `[YogaKit] Synthetic Browser: Read View RUM Session` | OK | expected drift (companion monitor) |

No unexpected drift among monitor *states*.

## SLOs

| SLO | Current SLI | Target (warn) | State | Error budget remaining (30d) |
|---|---|---|---|---|
| `[YogaKit] Read View Availability` (`read-view-availability`) | 18.669% | 99.5% (warn 99.9%) | **breached** | -16166.16% — see Findings #1, known/expected, do not re-open #39 |
| `[YogaKit] RUM Error-Free Sessions` (`rum-error-free-sessions`) | 100% | 99% (warn 99.5%) | ok | 100% (effectively no real-user session volume) |

## Core Web Vitals & RUM

- Real-user (non-synthetic) RUM traffic, last 24h: **0**. `@application.id:91af99b0-865b-405f-914e-bda170dc43b7`-scoped query for real-user sessions returned 0 buckets; the unscoped cross-check (`service:yogakit env:prod`, all sessions) confirms **96 sessions total, 100% `@session.type:synthetics` + `@browser.name:Chrome`** — the `read-view-rum-session` browser synthetic firing every ~15 min. Connector up, data genuinely absent — expected pre-launch state, unchanged from every prior sweep.
- LCP p75, real-user page-load p75 (used as INP proxy per this monitor family's legacy naming), RUM session-error rate: all null series (no real-user denominator) when scoped to non-synthetic sessions.
- Call: **NO-DATA** for real-user Core Web Vitals and RUM error rate (connector up, traffic genuinely absent, expected pre-launch, unchanged).

## API error rate & latency

Direct metric query against the `trace.web.request*` family (`api-error-rate.json`/`api-latency-p95.json`/`apm-telemetry-freshness.json`), last 24h, `service:yogakit env:prod`:

- Total requests: **1,729**, 5xx count: **0** (null series, i.e. never recorded) → server error rate **0%** (threshold: critical >5%, warn >2%) → **HEALTHY**
- p95 request duration: **≈42.7ms** (threshold: critical >5s, warn >2s) → **HEALTHY**
- All three monitors that query this family continue to read **OK**.

## Synthetic uptime

| Test | Last result | 24h | Notes |
|---|---|---|---|
| `homepage-200` (`tgu-u39-72k`) | passed (currently OK) | **1 failure** / 575 runs | same live-vs-manifest assertion disagreement as 2026-09-25 (see Findings #2) — auto-retried and recovered, no sustained outage |
| `poses-index-200` (`rq9-rxb-edd`) | passed | 0 failures / 574 runs | healthy |
| `read-view-200` (`z76-7mk-der`) | passed | 0 failures / 574 runs | healthy |
| `read-flow-offline` (browser, `qsv-emk-wqy`) | — | — | `status: paused` (stub, expected — not a failure) |

## Dashboard

`[YogaKit] Health` (`fpi-4fq-265`) — **reachable** via `GET /api/v1/dashboard/fpi-4fq-265` (title, description, and all three widget groups resolved).

## Config drift

`npm run datadog:drift-check` exited **0** — clean.

```
Validated 21 manifest(s). OK.
monitors: 11/11 manifest-backed entries in sync; 5 expected companion-monitor drift entries (Homepage/Poses/Read View synthetics + both browser tests) correctly classified as expected, not flagged.
slos: 2/2 in sync.
synthetics-api: Poses Index Returns 200, Read View Returns 200 in sync; Homepage Returns 200 — update, would apply (see Findings #2).
synthetics-browser: 2/2 in sync.
dashboards: 1/1 in sync.
logs-metrics: 1/1 in sync.
service-catalog: 1/1 in sync.
```

**Confirms `#86`'s `EXPECTED_DRIFT_MONITOR_NAMES` fix is live and correct** — this is the first sweep to exercise the fixed classifier: all 5 documented companion monitors are now correctly recognized as expected drift, `findUnexpectedDrift()` no longer spuriously exits 1 (see Findings #2 in the 2026-09-25 digest for the bug this closes).

## Findings

1. **SLO breach is the known historical outage continuing to age through the 30-day lookback — not a new incident, do not re-open #39.** `read-view-availability` SLI is 18.669% today (18.669% up from 16.912% on 2026-09-25), error budget -16166.16% (improving from -16517.6%). Both burn-rate monitors remain Alert. Unchanged narrative: the corrected SLO query (fixed 2026-09-23) honestly reflects the 2026-09-09–22 outage inside its 30-day window and will self-clear as it ages out (~2026-10-22). Current read-view availability is healthy — `read-view-200` synthetic passed all 574 runs in the last 24h, 0 failures.
2. **`findUnexpectedDrift()` classifier bug from 2026-09-25's sweep is fixed (`#86`/`ad90bdb`, confirmed live this sweep) — `npm run datadog:drift-check` now exits 0 cleanly.** The one remaining, genuine (non-classifier) drift persists unchanged from 2026-09-25: `homepage-200`'s live `responseTime` assertion (2000ms) still disagrees with the manifest's declared value (3000ms), even though `#80`/`4365caf` raised the manifest to 3000ms before yesterday's sweep — nobody has run `npm run datadog:apply` to push that manifest value live yet. This is exactly why the test logged its one 24h failure again today (1 of 575 runs, auto-retried, self-recovered). Record only — applying the fix is out of this command's scope.
3. No real-user RUM/CWV traffic (0 events beyond the `read-view-rum-session` synthetic), expected pre-launch, re-confirmed via the scoped/unscoped cross-check.
4. Everything else healthy: API error rate 0% / p95 ≈43ms, poses-index + read-view synthetics 100% passing, dashboard reachable, all monitor/SLO/synthetics manifests validated (21/21) with no unexpected drift.
