# /autoobs — 2026-09-21

**Sweep time:** 02:21 UTC
**Overall:** AT-RISK

Query path used: Datadog MCP tools directly (no `.env.local` present in this
container, so the `pup`/curl key-auth path in the command spec was unavailable;
MCP tools reach the same us5 org and are used here as an equivalent substitute,
not a degraded fallback — every figure below is a live query result, nothing
carried over from memory or a prior run).

## Monitors

16 monitors tagged `service:yogakit` found (11 match the 11 files under
`datadog/monitors/`; 5 extra are synthetic-test companion monitors —
documented "expected drift" per `datadog/README.md`).

- `[YogaKit] Server Error Rate > 5%` — OK
- `[YogaKit] Server Request Latency p95 > 5s` — OK
- `[YogaKit] Production Logs Missing` — OK
- `[YogaKit] APM Telemetry Missing for 30 Minutes` — OK
- `[YogaKit] Synthetic Test Runs Missing` — OK
- `[YogaKit] Read View Error Budget Consumed > 25%` (fast burn) — OK
- `[YogaKit] Read View Error Budget Consumed > 10%` (slow burn) — **Alert**
- `[YogaKit] Largest Contentful Paint p75 > 2.5s` — No Data (expected: sparse
  traffic, see Core Web Vitals section)
- `[YogaKit] Real-user Page Load p75 > 4s` — No Data (same as above)
- `[YogaKit] RUM Telemetry Missing for 30 Minutes` — No Data (same as above)
- `[YogaKit] RUM Error Rate > 5%` — No Data (same as above)
- `[YogaKit] Synthetic: Homepage Returns 200` — OK (expected drift: synthetic
  companion, matches `synthetics/api/homepage-200.json`)
- `[YogaKit] Synthetic: Poses Index Returns 200` — OK (expected drift, matches
  `synthetics/api/poses-index-200.json`)
- `[YogaKit] Synthetic: Read View Returns 200` — **Alert** (expected drift by
  origin, but the status itself is a real finding — see Synthetic uptime)
- `[YogaKit] Synthetic Browser: Read View RUM Session` — OK (expected drift)
- `[YogaKit] Synthetic Browser: Read Flow Offline` — No Data (expected: stub,
  `status: paused`, not a failure)

No unexpected/undocumented drift this sweep.

## SLOs

- `[YogaKit] Read View Availability` — SLI 99.89%, target 99.5% (warning
  threshold 99.9%), state **warning**, error budget remaining 77.7% (30d).
  Consistent with the slow-burn (>10%) monitor alerting; has not crossed the
  fast-burn (>25%) threshold.
- `[YogaKit] RUM Error-Free Sessions` — SLI 100%, target 99%, state OK, error
  budget remaining 100% (30d).

## Core Web Vitals & RUM

Last 24h, scoped `service:yogakit env:prod`, excluding synthetics —
96 real-user views/sessions (~4/hour; traffic exists but is sparse enough that
several 15-minute-window monitors above correctly read "No Data" rather than
"OK" — checked both scoped and unscoped counts per the command's guidance,
this is genuinely sparse traffic, not a RUM tagging bug: unscoped 24h RUM
event volume is 2,815, consistent with occasional real sessions).

- LCP p75: ≈378ms — **HEALTHY** (threshold 2,500ms)
- Loading time p75 (the metric the "INP" monitor actually measures): ≈893ms —
  **HEALTHY** (threshold 4,000ms)
- RUM error rate: 0 / 96 sessions with a client error — **HEALTHY** (threshold
  5%)

## API error rate & latency

Last 24h, `service:yogakit env:prod`, all spans (18,754 total):

- p95 latency: ≈23.9ms — **HEALTHY** (threshold 5,000ms)
- 5xx error spans: 0 — **HEALTHY** (threshold 5%)

## Synthetic uptime

- Homepage (`tgu-u39-72k`, `/`) — OK, last run 2026-09-20T20:41:19Z
- Poses Index (`rq9-rxb-edd`, `/poses`) — OK
- **Read View (`z76-7mk-der`, `/read/classic-yin-full-body`) — FAILING.**
  Every result in the last 24h (both `aws:us-east-1` and `aws:us-west-2`,
  including retries) fails the body-content assertion. Expected body to
  contain `"60-Minute Yin: Classic Full Body"`; actual response begins
  `<!DOCTYPE html><html data-dpl-id="dpl_AmjfgFPaTyPr"...` — this looks like a
  Vercel deployment-protection/interstitial page being served instead of the
  real read view, not a content regression in the page itself. Status code
  and response-time assertions were not reported as the failure cause; only
  the body-content assertion failed.
- Read Flow Offline (browser, `qsv-emk-wqy`) — paused as expected, not a
  failure.

## Dashboard

`[YogaKit] Health` (`fpi-4fq-265`) — reachable via `GET /api/v1/dashboard/`.

## Findings

1. **AT-RISK — Read View synthetic uptime test failing 100% of runs, both
   probe locations, over the full 24h window.** The public read view at
   `/read/classic-yin-full-body` — the core path RULE-L3/L4 (the "6am test")
   is built around — is either behind a Vercel deployment-protection wall or
   otherwise not returning expected content to an unauthenticated prober.
   This is the same root cause driving the SLO slow-burn alert above (SLI
   99.89% vs 99.5% target, warning state). Record only, per this command's
   scope — no fix attempted. Needs an operator to check Vercel deployment
   protection / preview-protection settings on the `yogakit.vercel.app`
   production alias.
2. Sparse RUM traffic (96 sessions/24h) means several Web Vitals/RUM monitors
   read "No Data" rather than "OK" on their 15–30 minute evaluation windows —
   expected given traffic volume, not itself a finding, but worth noting
   nothing else in this sweep needed the degraded-connector fallback: none.

## Out of scope this run (see final reply)

- `/daily-connector-report` and the "briefing pipeline"
  (briefing-compose → briefing-daily-dispatch → first-sync-on-connect →
  integration-health-check) named in this run's schedule do not exist
  anywhere in this repo — no command file, no skill, no GitHub Actions
  workflow, no Supabase tables/functions by those names. Not run; not
  fabricated. See final reply for detail.

## Re-run 08:12 UTC

Second sweep this UTC day, ~6h after the 02:21 run. `.claude/commands/tools/03_observation/autoobs.md`
(cited by this run's schedule as the authoritative, hardened spec) still does not exist in this
repo — only `.claude/commands/autoobs.md` does, which is what this re-run followed. Same query
path as the first sweep: no `.env.local`, so Datadog MCP tools were used directly (us5 org),
not `pup`/curl key-auth. Every figure below is a fresh query result from this session.

### Monitors

16 monitors tagged `service:yogakit` (11 manifest-matched, 5 expected synthetic-companion drift,
same as the 02:21 sweep). Status unchanged from the first sweep:

- All 9 previously-OK monitors — still **OK** (server error rate, server latency p95, production
  logs, APM telemetry freshness, synthetic test runs missing, SLO fast-burn, homepage/poses-index
  synthetics, read-view RUM-session synthetic).
- `[YogaKit] Read View Error Budget Consumed > 10%` (slow burn) — still **Alert**.
- `[YogaKit] Synthetic: Read View Returns 200` — still **Alert**.
- LCP p75, Real-user Page Load p75, RUM Telemetry Missing, RUM Error Rate — still **No Data**
  (genuinely 0 non-synthetic RUM traffic this window — see Core Web Vitals below, this is now
  confirmed, not assumed).
- `Read Flow Offline` browser test — still **No Data**, `status: paused`, not a failure.

No unexpected drift.

### SLOs

- `[YogaKit] Read View Availability` — SLI 99.89%, target 99.5% (warning 99.9%), state
  **warning**, error budget remaining 78.11% (30d) — essentially unchanged from 77.7% at 02:21.
- `[YogaKit] RUM Error-Free Sessions` — SLI 100%, target 99%, state OK, error budget 100%.

### Core Web Vitals & RUM — correction to the 02:21 entry

Last 24h, `@application.id:91af99b0-865b-405f-914e-bda170dc43b7`: 2,795 total RUM events, broken
down as 96 `session` + 96 `view` + 2,345 `resource` + 258 `long_task`. Grouping the 96 sessions by
`@session.type` shows **all 96 are `synthetics`** — the `[YogaKit] Synthetic Browser: Read View RUM
Session` companion monitor firing every 15 minutes, not organic traffic. A query for
`@type:session -@session.type:synthetics` (real users only) returns **0 buckets, 0 sessions**,
confirmed twice (with and without the `service`/`env` tag filter) — this is genuinely no
real-user traffic, not a RUM tagging bug (the "check both scoped and unscoped" guidance in the
command spec was followed; both agree on zero).

This means the 02:21 digest's "96 real-user views/sessions... HEALTHY, LCP p75 ≈378ms" line was
**wrong** — it did not exclude `@session.type:synthetics`, so it reported the synthetic
companion monitor's traffic as if it were real users. There is currently no real-user LCP, INP,
or RUM-error-rate figure to report — **NO-DATA**, consistent with (and now explaining) the four
"No Data" monitors above. This is not a new incident; it matches the RULE-L3/L4 pre-launch
traffic profile already known from the 2026-09-04 sweep.

### API error rate & latency

Last 24h, `service:yogakit env:prod`, 18,775 spans:

- p95 latency: ≈25.4ms — **HEALTHY** (threshold 5,000ms)
- 5xx error spans: 0 — **HEALTHY** (threshold 5%)

### Synthetic uptime

- Homepage (`tgu-u39-72k`) — **OK**, all sampled runs in the last 24h passed.
- Poses Index (`rq9-rxb-edd`) — **OK**, all sampled runs in the last 24h passed.
- Read View (`z76-7mk-der`) — **still FAILING**. All sampled results in the last 24h (both
  `aws:us-east-1` and `aws:us-west-2`) fail the same body-content assertion; response begins
  `<!DOCTYPE html><html data-dpl-id="dpl_EhkVEeCcaWUq"...` (a different `dpl_` id than the 02:21
  sweep's `dpl_AmjfgFPaTyPr`, i.e. a new deployment since then — still shows the same
  interstitial/protection page, so the underlying condition has not cleared across a redeploy).
- Read Flow Offline (browser, `qsv-emk-wqy`) — confirmed `status: paused` via test config, not a
  failure.

### Dashboard

`[YogaKit] Health` (`fpi-4fq-265`) — reachable via `GET /api/v1/dashboard/fpi-4fq-265`.

### `/daily-connector-report` and the briefing pipeline — independently re-verified absent

Re-checked this run, not carried over from the 02:21 entry: `mcp__Supabase__list_edge_functions`
on the `yogakit` project (`ydvxgjhvblrlnjqysoko`) returns zero functions; `.github/workflows/`
contains only `ci.yml` and `supabase-branch.yml`; a repo-wide grep for `briefing-compose`,
`briefing-daily-dispatch`, `first-sync-on-connect`, `integration-health-check` matches nothing
outside this digest and the schedule-derived `.claude/commands/autoobs.md`/autopm-digest text
itself (a few unrelated hits for the word "briefing" in `VOICE.md`/`DECISIONS.md`/`specs/009-*`
refer to NextMove's writing-voice conventions, not a pipeline). No command, skill, GitHub Actions
workflow, or Supabase function by any of these names exists in this repo. Verdict: **cannot
assess** — the named pipeline has not been built here. Not a connector-dark degrade; there is
nothing to query.

### Findings (re-run)

1. Same as 02:21: Read View synthetic still failing 100% of runs across both probe locations,
   now confirmed across a redeploy (`dpl_` id changed) — this is not a one-off deploy artifact,
   it is a persistent condition. Still consistent with the SLO warning state. Record only.
2. **New this re-run**: the 02:21 digest's RUM figures ("96 real-user sessions, HEALTHY") were a
   measurement error — all 96 sessions were synthetic. Real-user RUM traffic is 0 for the full
   24h window sampled by both sweeps. Corrected above; no fix implied, record only.
3. `/daily-connector-report` and the briefing pipeline remain unbuilt — same as 02:21, now
   independently re-verified via Supabase and GitHub Actions rather than assumed.

## Re-run 16:59 UTC

Third sweep this UTC day, following the scheduled overnight-health-sweep prompt. This time
`.claude/commands/tools/03_observation/autoobs.md` (the hardened spec cited by the schedule)
does exist in this repo and was followed directly, including its query-access order (`pup`
CLI first, key-auth via `DD_API_KEY`/`DD_APP_KEY`/`DD_SITE` present as environment variables
in this container — no `.env.local` file, but the variables themselves resolved, so both `pup`
and the Datadog MCP tools were available this run). `NEXT_PUBLIC_DD_RUM_APPLICATION_ID` was not
present in the environment either; resolved instead via `search_rum_applications` (name
"yogakit") → `91af99b0-865b-405f-914e-bda170dc43b7`, matching the ID used by the two prior
sweeps today. Every figure below is a fresh query result from this session.

### Monitors

16 monitors tagged `service:yogakit`, queried via `pup monitors list --tags='service:yogakit'`
(11 manifest-matched, 5 expected synthetic-companion drift, unchanged). Status unchanged from
both earlier sweeps today:

- 9 previously-OK monitors — still **OK**: server error rate, server latency p95, production
  logs, APM telemetry freshness, synthetic test runs missing, SLO fast-burn, homepage synthetic,
  poses-index synthetic, read-view RUM-session synthetic.
- `[YogaKit] Read View Error Budget Consumed > 10%` (slow burn) — still **Alert**.
- `[YogaKit] Synthetic: Read View Returns 200` — still **Alert**.
- LCP p75, Real-user Page Load p75, RUM Telemetry Missing, RUM Error Rate — still **No Data**
  (0 real-user RUM traffic this window, confirmed below — not a tagging bug).
- `Read Flow Offline` browser test — still **No Data**, `status: paused`, not a failure.

No unexpected drift.

### SLOs

Queried via `pup slos status <id> --from=30d --to=now`:

- `[YogaKit] Read View Availability` — SLI 99.89%, target 99.5% (warning 99.9%), state
  **warning**, error budget remaining 78.70% (30d) — essentially flat vs. 78.11% at 08:12 and
  77.7% at 02:21 (burn has not materially accelerated since this morning, but has also not
  recovered).
- `[YogaKit] RUM Error-Free Sessions` — SLI 100%, target 99%, state OK, error budget 100%
  remaining.

### Core Web Vitals & RUM

Last 24h, `@application.id:91af99b0-865b-405f-914e-bda170dc43b7 @type:view`/`@type:session`,
excluding `session_type:synthetics` and `browser:HeadlessChrome`:

- View count (real-user): **0 buckets** — no LCP p75 or loading-time p75 to report, **NO-DATA**.
- Session count (real-user): **0 buckets**.
- Unscoped cross-check (`service:yogakit env:prod @type:session -@session.type:synthetics`, no
  `@application.id` filter): **also 0 buckets**. Scoped and unscoped agree at zero — per the
  command's guidance this confirms genuinely no real-user traffic, not a RUM tagging bug.
- RUM error rate: no sessions to compute a rate over — **NO-DATA**, not BREACHED.

Consistent with the 08:12 correction and the 2026-09-04 pre-launch baseline: no organic traffic
yet, only the 15-minute synthetic RUM-session companion (which the monitors above correctly
exclude).

### API error rate & latency

Last 24h, `service:yogakit env:prod`, 18,887 spans:

- p95 latency: ≈25.4ms — **HEALTHY** (threshold 5,000ms)
- 5xx error spans: 0 — **HEALTHY** (threshold 5%)

### Synthetic uptime

- Homepage (`tgu-u39-72k`) — **OK**, all 10 most-recent sampled runs (both `aws:us-east-1` and
  `aws:us-west-2`) passed.
- Poses Index (`rq9-rxb-edd`) — **OK**, all 10 most-recent sampled runs passed.
- Read View (`z76-7mk-der`) — **still FAILING**. All 10 most-recent sampled runs (both
  probe locations) fail the same body-content assertion; response begins
  `<!DOCTYPE html><html data-dpl-id="dpl_DY7FnzrB83VG"...` — a third distinct `dpl_` id today
  (`dpl_AmjfgFPaTyPr` at 02:21, `dpl_EhkVEeCcaWUq` at 08:12), meaning at least one more
  production deploy has happened since the last sweep and the interstitial/protection page is
  still being served every time. Persistent across three redeploys now — not a one-off.
- Read Flow Offline (browser, `qsv-emk-wqy`) — `status: paused`, not a failure.

### Dashboard

`[YogaKit] Health` (`fpi-4fq-265`) — reachable via `GET /api/v1/dashboard/fpi-4fq-265`
(`search_datadog_dashboards`/`get_datadog_dashboard` resolved title and metadata successfully).

### Findings (16:59 re-run)

1. **AT-RISK, unchanged and now confirmed across three redeploys**: the public read view at
   `/read/classic-yin-full-body` fails its synthetic content-assertion check 100% of sampled
   runs, both probe locations, for the third sweep in a row today, across three different
   `dpl_` deployment ids. This is not a transient blip — it is a persistent condition that has
   survived at least three production deploys since 02:21 UTC. Driving the SLO slow-burn alert
   (warning state, 78.70% budget remaining, essentially flat since this morning). Still looks
   like a Vercel deployment-protection/interstitial page being served to the unauthenticated
   synthetic prober rather than a content regression in the read view itself. Record only, per
   this command's scope — needs an operator to check Vercel deployment-protection settings on
   the production alias. **No escalation issue was found open for this** despite two prior
   AT-RISK verdicts today (02:21, 08:12) — see Escalation below.
2. Real-user RUM traffic remains 0 for the full 24h window, third sweep confirming the same —
   expected pre-launch state (RULE-L3/L4), not itself a new finding.

### Escalation

Searched `label:auto/observation` (open and unfiltered) and title text before this run — zero
results either way, despite two AT-RISK verdicts earlier today. No prior escalation exists to
update. Opening a new issue now per this run's AT-RISK verdict; self-verified after posting (see
final reply).
