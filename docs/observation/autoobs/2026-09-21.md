# /autoobs — 2026-09-21

**Sweep time:** 02:21 UTC
**Overall:** AT-RISK

Query path used: Datadog MCP tools directly (no `.env.local` present in this
container, so the `pup`/curl key-auth path in the command spec was unavailable;
MCP tools reach the same us5 org and are used here as an equivalent substitute,
not a degraded fallback — every figure below is a live query result, nothing
carried over from memory or a prior run).

## Monitors

16 monitors tagged `service:yogakit` found (11 match the 11 files under
`datadog/monitors/`; 5 extra are synthetic-test companion monitors —
documented "expected drift" per `datadog/README.md`).

- `[YogaKit] Server Error Rate > 5%` — OK
- `[YogaKit] Server Request Latency p95 > 5s` — OK
- `[YogaKit] Production Logs Missing` — OK
- `[YogaKit] APM Telemetry Missing for 30 Minutes` — OK
- `[YogaKit] Synthetic Test Runs Missing` — OK
- `[YogaKit] Read View Error Budget Consumed > 25%` (fast burn) — OK
- `[YogaKit] Read View Error Budget Consumed > 10%` (slow burn) — **Alert**
- `[YogaKit] Largest Contentful Paint p75 > 2.5s` — No Data (expected: sparse
  traffic, see Core Web Vitals section)
- `[YogaKit] Real-user Page Load p75 > 4s` — No Data (same as above)
- `[YogaKit] RUM Telemetry Missing for 30 Minutes` — No Data (same as above)
- `[YogaKit] RUM Error Rate > 5%` — No Data (same as above)
- `[YogaKit] Synthetic: Homepage Returns 200` — OK (expected drift: synthetic
  companion, matches `synthetics/api/homepage-200.json`)
- `[YogaKit] Synthetic: Poses Index Returns 200` — OK (expected drift, matches
  `synthetics/api/poses-index-200.json`)
- `[YogaKit] Synthetic: Read View Returns 200` — **Alert** (expected drift by
  origin, but the status itself is a real finding — see Synthetic uptime)
- `[YogaKit] Synthetic Browser: Read View RUM Session` — OK (expected drift)
- `[YogaKit] Synthetic Browser: Read Flow Offline` — No Data (expected: stub,
  `status: paused`, not a failure)

No unexpected/undocumented drift this sweep.

## SLOs

- `[YogaKit] Read View Availability` — SLI 99.89%, target 99.5% (warning
  threshold 99.9%), state **warning**, error budget remaining 77.7% (30d).
  Consistent with the slow-burn (>10%) monitor alerting; has not crossed the
  fast-burn (>25%) threshold.
- `[YogaKit] RUM Error-Free Sessions` — SLI 100%, target 99%, state OK, error
  budget remaining 100% (30d).

## Core Web Vitals & RUM

Last 24h, scoped `service:yogakit env:prod`, excluding synthetics —
96 real-user views/sessions (~4/hour; traffic exists but is sparse enough that
several 15-minute-window monitors above correctly read "No Data" rather than
"OK" — checked both scoped and unscoped counts per the command's guidance,
this is genuinely sparse traffic, not a RUM tagging bug: unscoped 24h RUM
event volume is 2,815, consistent with occasional real sessions).

- LCP p75: ≈378ms — **HEALTHY** (threshold 2,500ms)
- Loading time p75 (the metric the "INP" monitor actually measures): ≈893ms —
  **HEALTHY** (threshold 4,000ms)
- RUM error rate: 0 / 96 sessions with a client error — **HEALTHY** (threshold
  5%)

## API error rate & latency

Last 24h, `service:yogakit env:prod`, all spans (18,754 total):

- p95 latency: ≈23.9ms — **HEALTHY** (threshold 5,000ms)
- 5xx error spans: 0 — **HEALTHY** (threshold 5%)

## Synthetic uptime

- Homepage (`tgu-u39-72k`, `/`) — OK, last run 2026-09-20T20:41:19Z
- Poses Index (`rq9-rxb-edd`, `/poses`) — OK
- **Read View (`z76-7mk-der`, `/read/classic-yin-full-body`) — FAILING.**
  Every result in the last 24h (both `aws:us-east-1` and `aws:us-west-2`,
  including retries) fails the body-content assertion. Expected body to
  contain `"60-Minute Yin: Classic Full Body"`; actual response begins
  `<!DOCTYPE html><html data-dpl-id="dpl_AmjfgFPaTyPr"...` — this looks like a
  Vercel deployment-protection/interstitial page being served instead of the
  real read view, not a content regression in the page itself. Status code
  and response-time assertions were not reported as the failure cause; only
  the body-content assertion failed.
- Read Flow Offline (browser, `qsv-emk-wqy`) — paused as expected, not a
  failure.

## Dashboard

`[YogaKit] Health` (`fpi-4fq-265`) — reachable via `GET /api/v1/dashboard/`.

## Findings

1. **AT-RISK — Read View synthetic uptime test failing 100% of runs, both
   probe locations, over the full 24h window.** The public read view at
   `/read/classic-yin-full-body` — the core path RULE-L3/L4 (the "6am test")
   is built around — is either behind a Vercel deployment-protection wall or
   otherwise not returning expected content to an unauthenticated prober.
   This is the same root cause driving the SLO slow-burn alert above (SLI
   99.89% vs 99.5% target, warning state). Record only, per this command's
   scope — no fix attempted. Needs an operator to check Vercel deployment
   protection / preview-protection settings on the `yogakit.vercel.app`
   production alias.
2. Sparse RUM traffic (96 sessions/24h) means several Web Vitals/RUM monitors
   read "No Data" rather than "OK" on their 15–30 minute evaluation windows —
   expected given traffic volume, not itself a finding, but worth noting
   nothing else in this sweep needed the degraded-connector fallback: none.

## Out of scope this run (see final reply)

- `/daily-connector-report` and the "briefing pipeline"
  (briefing-compose → briefing-daily-dispatch → first-sync-on-connect →
  integration-health-check) named in this run's schedule do not exist
  anywhere in this repo — no command file, no skill, no GitHub Actions
  workflow, no Supabase tables/functions by those names. Not run; not
  fabricated. See final reply for detail.
