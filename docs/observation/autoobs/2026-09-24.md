# /autoobs — 2026-09-24

**Sweep time:** 08:22 UTC
**Overall:** HEALTHY

## Monitors

16 monitors tagged `service:yogakit` (11 matching `datadog/monitors/*.json` manifests + 5 expected drift — Datadog's auto-created synthetics companion monitors, per `datadog/README.md` "Expected drift"). 6 OK / 2 Alert / 8 No Data.

| Monitor | Status | Manifest |
|---|---|---|
| `[YogaKit] Server Error Rate > 5%` | No Data | `api-error-rate.json` — see Findings #2, live traffic confirmed healthy via direct metric query |
| `[YogaKit] Server Request Latency p95 > 5s` | No Data | `api-latency-p95.json` — see Findings #2 |
| `[YogaKit] RUM Error Rate > 5%` | No Data | `rum-error-rate.json` — 0 real-user RUM sessions, expected pre-launch |
| `[YogaKit] Synthetic Test Runs Missing` | OK | `synthetic-read-view-down.json` |
| `[YogaKit] Real-user Page Load p75 > 4s` | No Data | `web-vitals-inp.json` (legacy filename) — 0 real-user sessions |
| `[YogaKit] Largest Contentful Paint p75 > 2.5s` | No Data | `web-vitals-lcp.json` — 0 real-user sessions |
| `[YogaKit] Production Logs Missing` | OK | `log-ingestion-liveness.json` |
| `[YogaKit] Read View Error Budget Consumed > 25%` (fast burn) | **Alert** | `slo-burn-fast-read-view-availability.json` — see Findings #1, known/expected |
| `[YogaKit] Read View Error Budget Consumed > 10%` (slow burn) | **Alert** | `slo-burn-slow-read-view-availability.json` — see Findings #1, known/expected |
| `[YogaKit] APM Telemetry Missing for 30 Minutes` | No Data | `apm-telemetry-freshness.json` — see Findings #2 |
| `[YogaKit] RUM Telemetry Missing for 30 Minutes` | No Data | `rum-telemetry-freshness.json` |
| `[YogaKit] Synthetic: Homepage Returns 200` | OK | expected drift (companion monitor) — see Findings #3 |
| `[YogaKit] Synthetic: Poses Index Returns 200` | OK | expected drift (companion monitor) |
| `[YogaKit] Synthetic: Read View Returns 200` | OK | expected drift (companion monitor) |
| `[YogaKit] Synthetic Browser: Read Flow Offline` | No Data (paused, expected) | expected drift (companion monitor) |
| `[YogaKit] Synthetic Browser: Read View RUM Session` | OK | expected drift (companion monitor) |

No unexpected drift observed.

## SLOs

| SLO | Current SLI | Target (warn) | State | Error budget remaining (30d) |
|---|---|---|---|---|
| `[YogaKit] Read View Availability` (`read-view-availability`) | 15.096% | 99.5% (warn 99.9%) | **breached** | -16880.8% — see Findings #1, known/expected, do not re-open #39 |
| `[YogaKit] RUM Error-Free Sessions` (`rum-error-free-sessions`) | 100% | 99% (warn 99.5%) | ok | 100% (effectively no real-user session volume) |

## Core Web Vitals & RUM

- Real-user (non-synthetic) RUM sessions, last 24h: **0**, confirmed both scoped (`@application.id:91af99b0-865b-405f-914e-bda170dc43b7`) and unscoped — both read 96 views, all `@session.type:synthetics` + `@browser.name:Chrome` (the `read-view-rum-session` browser synthetic firing every ~15 min). Connector up, data genuinely absent — expected pre-launch state, unchanged from every prior sweep.
- Call: **NO-DATA** for real-user Core Web Vitals and RUM error rate (connector up, traffic genuinely absent, expected pre-launch, unchanged).

## API error rate & latency

Datadog's derived-metric monitors (`api-error-rate.json`, `api-latency-p95.json`, `apm-telemetry-freshness.json`) read **No Data**. Cross-checked directly against the `trace.web.request*` metric family (the monitors query `trace.next_js.BaseServer.handleRequest*`, which does not appear at all in `search_datadog_metrics` for `service:yogakit` over the last 7 days — see Findings #2), last 24h, `service:yogakit env:prod`:

- Total requests: **1,746**, 5xx count: **0** (null series, i.e. never recorded) → server error rate **0%** (threshold: critical >5%, warn >2%) → **HEALTHY**
- p95 request duration: **≈248ms** (threshold: critical >5s, warn >2s) → **HEALTHY**

## Synthetic uptime

| Test | Last result | 24h | Notes |
|---|---|---|---|
| `homepage-200` (`tgu-u39-72k`) | passed | 2 transient failures (responseTime >2000ms: 2264ms, 2005ms, both `aws:us-west-2`, both auto-retried), currently OK | minor, informational — see Findings #3 |
| `poses-index-200` (`rq9-rxb-edd`) | passed | 0 failures / 24h | healthy |
| `read-view-200` (`z76-7mk-der`) | passed | 0 failures / 574 runs, both locations | healthy |
| `read-flow-offline` (browser, `qsv-emk-wqy`) | — | — | `status: paused` (stub, expected — not a failure) |

## Dashboard

`[YogaKit] Health` (`fpi-4fq-265`) — **reachable** via `GET /api/v1/dashboard/fpi-4fq-265`.

## Findings

1. **SLO now correctly reporting the known historical outage — not a new incident, do not re-open #39.** `read-view-availability` SLI dropped from 99.902% (2026-09-23 08:21 sweep) to 15.096% today, and both burn-rate monitors flipped from OK/1-Alert to 2-Alert. This is *not* a regression: the SLO manifest's `modified_at` (1790173325 → 2026-09-23T14:22:04Z, per the two burn-rate monitors' own `modified` timestamps) shows the query was corrected several hours *after* yesterday's sweep, per `DECISIONS.md` (2026-09-23 entry) and the monitors' own messages: the original query scoped to `status_code_class:2xx`, blind to the body-assertion failures behind the 13-day outage (2026-09-09–09-22, root-caused and fixed in PR #54/#52). The corrected query now honestly reflects that historical outage inside the 30-day lookback and will self-clear as it ages out (~2026-10-22). Actual current read-view availability is healthy — `read-view-200` synthetic passed all 574 runs in the last 24h.
2. **Monitor/derived-metric discrepancy, not a real gap — refines 2026-09-23 Findings #3.** `api-error-rate`, `api-latency-p95`, and `apm-telemetry-freshness` monitors query `trace.next_js.BaseServer.handleRequest*`. `search_datadog_metrics` (name filter `trace.next_js`, tag `service:yogakit`, 7d lookback) shows that metric family does **not exist** at all for this service — only `trace.next_js.Middleware.execute*`, `trace.next_js.AppRender.getBodyResult*`, `trace.next_js.NextNodeServer.startResponse*`, `trace.web.request*`, and `trace.browser.request*` are present. Yesterday's sweep guessed "sparse traffic reading as No Data"; today's direct lookup narrows it to a likely metric-name/instrumentation-version mismatch in the monitor manifests. Underlying traffic via `trace.web.request*` is healthy (1,746 requests/24h, 0% errors, p95 ≈248ms). Worth a follow-up to retarget the three monitor queries at `trace.web.request*` — record only, not fixed here (out of this command's scope).
3. **Two transient `homepage-200` failures, self-recovered.** Both were `responseTime > 2000ms` assertion misses from `aws:us-west-2` only (2264ms, 2005ms), each auto-retried; monitor is currently OK. Not a pattern (0 failures on the other two API synthetics), informational only.
4. No real-user RUM traffic (0 sessions, last 24h) — expected pre-launch state, unchanged, re-confirmed via the scoped/unscoped cross-check.
5. Everything else healthy: poses-index + read-view synthetics 100% passing, dashboard reachable, no unexpected monitor drift.
