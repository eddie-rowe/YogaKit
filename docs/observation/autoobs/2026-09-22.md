# /autoobs — 2026-09-22

**Sweep time:** 08:05 UTC
**Overall:** AT-RISK

## Monitors

16 monitors tagged `service:yogakit` (11 matching `datadog/monitors/*.json` manifests + 5 expected drift — Datadog's auto-created synthetics companion monitors, per `datadog/README.md` "Expected drift"). 9 OK / 2 Alert / 5 No Data.

| Monitor | Status | Manifest |
|---|---|---|
| `[YogaKit] Server Error Rate > 5%` | OK | `api-error-rate.json` |
| `[YogaKit] Server Request Latency p95 > 5s` | OK | `api-latency-p95.json` |
| `[YogaKit] RUM Error Rate > 5%` | No Data | `rum-error-rate.json` — 0 real-user RUM sessions, see below |
| `[YogaKit] Synthetic Test Runs Missing` | OK | `synthetic-read-view-down.json` |
| `[YogaKit] Real-user Page Load p75 > 4s` | No Data | `web-vitals-inp.json` (legacy filename) — 0 real-user sessions |
| `[YogaKit] Largest Contentful Paint p75 > 2.5s` | No Data | `web-vitals-lcp.json` — 0 real-user sessions |
| `[YogaKit] Production Logs Missing` | OK | `log-ingestion-liveness.json` |
| `[YogaKit] Read View Error Budget Consumed > 25%` (fast burn) | OK | `slo-burn-fast-read-view-availability.json` |
| `[YogaKit] Read View Error Budget Consumed > 10%` (slow burn) | **Alert** | `slo-burn-slow-read-view-availability.json` |
| `[YogaKit] APM Telemetry Missing for 30 Minutes` | No Data | `apm-telemetry-freshness.json` |
| `[YogaKit] RUM Telemetry Missing for 30 Minutes` | No Data | `rum-telemetry-freshness.json` |
| `[YogaKit] Synthetic: Homepage Returns 200` | OK | expected drift (companion monitor) |
| `[YogaKit] Synthetic: Poses Index Returns 200` | OK | expected drift (companion monitor) |
| `[YogaKit] Synthetic: Read View Returns 200` | **Alert** | expected drift (companion monitor) |
| `[YogaKit] Synthetic Browser: Read Flow Offline` | No Data (paused, expected) | expected drift (companion monitor) |
| `[YogaKit] Synthetic Browser: Read View RUM Session` | OK | expected drift (companion monitor) |

No unexpected drift observed.

## SLOs

| SLO | Current SLI | Target (warn) | State | Error budget remaining (30d) |
|---|---|---|---|---|
| `[YogaKit] Read View Availability` (`read-view-availability`) | 99.8982% | 99.5% (warn 99.9%) | **warning** | 79.64% |
| `[YogaKit] RUM Error-Free Sessions` (`rum-error-free-sessions`) | 100% | 99% (warn 99.5%) | ok | 100% (raw denominator ~0.37 "requests" over 30d — effectively no real-user session volume) |

## Core Web Vitals & RUM

- Real-user (non-synthetic) RUM sessions, last 24h: **0**, confirmed both scoped (`@application.id:91af99b0-865b-405f-914e-bda170dc43b7`, `NEXT_PUBLIC_DD_RUM_APPLICATION_ID`) and unscoped (`service:yogakit env:prod`) — both read 96 raw sessions, and grouping by `@session.type`/`@browser.name` shows **all 96 are `session.type:synthetics`, `browser:Chrome`** — the `read-view-rum-session` browser synthetic firing every 15 min (96 × 15min = 24h), exactly as documented in that monitor's message ("generate a real RUM session... before organic traffic exists"). This is **connector UP, data genuinely absent** — real anonymous traffic is still zero, expected pre-launch state, not a new finding.
- LCP p75 / page-load-time p75 computed over the synthetic-only traffic above (informational, not what the monitors evaluate): LCP p75 ≈ 0.34s, loading-time p75 ≈ 0.81s — both well under threshold, but this reflects the synthetic prober's own performance, not real users.
- RUM error rate: NO-DATA for real users (0 sessions); 0 error sessions among the synthetic-only traffic.
- Call: **NO-DATA** (real-user Core Web Vitals and RUM error rate — connector up, traffic genuinely absent, expected pre-launch).

## API error rate & latency

- Server error rate, last 24h: **0%** (threshold: critical >5%, warn >2%) → **HEALTHY**
- Server p95 latency, last 24h: **≈47ms** (threshold: critical >5s, warn >2s) → **HEALTHY**

## Synthetic uptime

| Test | Last result | 24h | Notes |
|---|---|---|---|
| `homepage-200` (`tgu-u39-72k`) | passed | 576/576 passed (100%) | healthy |
| `poses-index-200` (`rq9-rxb-edd`) | passed | 574/574 passed (100%) | healthy |
| `read-view-200` (`z76-7mk-der`) | **failed** | 100% of sampled result-level runs failed, both locations (`aws:us-east-1`, `aws:us-west-2`) | HTTP status is 200 (2xx) and response-time assertion passes — only the **body-content assertion** fails. Response body begins `<!DOCTYPE html><html data-dpl-id="dpl_oqYK5rHNYuri"...`, not the expected `"60-Minute Yin: Classic Full Body"` — looks like a Vercel deployment-protection/interstitial page, not a content regression. Same signature already tracked in issue #39, now confirmed persisting into a further deployment (`dpl_oqYK5rHNYuri`). |
| `read-flow-offline` (browser, `qsv-emk-wqy`) | — | — | `status: paused` (stub, expected — not a failure) |

## Dashboard

`[YogaKit] Health` (`fpi-4fq-265`) — **reachable** via `GET /api/v1/dashboard/fpi-4fq-265`.

## Findings

1. **AT-RISK — read view synthetic still failing (persists across another deployment).** `read-view-200` (`z76-7mk-der`) fails its body-content assertion 100% of sampled runs, both probe locations, on the latest production deployment (`dpl_oqYK5rHNYuri`). Same failure signature (200 status, wrong body — looks like a Vercel deployment-protection interstitial) already tracked by open issue [#39](https://github.com/eddie-rowe/YogaKit/issues/39), opened 2026-09-21 after three same-day sweeps confirmed it across three prior deployments. This sweep confirms it is still occurring on a fourth. Downstream effects, all consistent with prior sweeps and not new: `[YogaKit] Synthetic: Read View Returns 200` monitor in Alert; `[YogaKit] Read View Error Budget Consumed > 10%` (slow-burn) monitor in Alert; `read-view-availability` SLO in `warning` state (SLI 99.8982%, 79.64% error budget remaining, essentially flat vs. yesterday's last reading of 78.70%).
2. No real-user RUM traffic (0 sessions, last 24h) — expected pre-launch state per the `read-view-rum-session` synthetic's own documented purpose; not a new finding, re-confirmed via the scoped/unscoped cross-check.
3. Everything else healthy: API error rate 0%, API p95 latency ≈47ms, homepage + poses-index synthetics 100% passing (576/576, 574/574), dashboard reachable, no unexpected monitor drift.
