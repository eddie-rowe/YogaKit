# /autopm — 2026-09-22

**Run time:** ~09:20 UTC
**Spec followed:** `.claude/commands/tools/01_project_management/autopm.md` (the real, hardened
spec — second sweep against it, first was 2026-09-21 17:06)

## 1. Project snapshot

**Audit verdict: AT-RISK**, unchanged from this morning's `/autoobs` (08:05 UTC,
`docs/observation/autoobs/2026-09-22.md`). Read-view synthetic (`z76-7mk-der`,
`/read/classic-yin-full-body`) is still failing its body-content assertion 100% of sampled
runs, both probe locations, now confirmed across a **4th production deployment**
(`dpl_oqYK5rHNYuri`). HTTP status is a clean 200 every time — response body looks like a
Vercel deployment-protection interstitial, not a code regression. Downstream: `slow-burn`
SLO monitor in Alert, `read-view-availability` SLO in `warning` (79.64% error budget
remaining, flat vs. yesterday). Everything else observed is healthy: API error rate 0%,
p95 latency ≈47ms, homepage + poses-index synthetics 100% passing, 0 unexpected monitor
drift. Already tracked on `auto/observation` #39 (updated in place, not re-escalated).

Top focus area: keep the dev lane fed while #39's blocker (Vercel deployment-protection
setting, needs operator dashboard access) sits with the owner — not something this loop can
close itself.

## 2. Board health

Query: `mcp__github__list_issues` (open/closed), `mcp__github__list_pull_requests`
(open/closed/dependabot).

- **Closed in last 48h:** 3 — `#35` (npm audit), `#36` (ESLint entities/links), `#37`
  (ESLint set-state-in-effect), all via merged PRs `#42`/`#43`/`#44` (2026-09-21
  ~18:10–18:14 UTC).
- **Open PRs:** 0. **Open Dependabot PRs:** 0 — nothing to flag for batch-merge this sweep.
- **Open issues at sweep start:** 4 — `#38` (owner-digest), `#39` (auto/observation),
  `#40` (`ready-for-dev`+`auto-ok`, feat 004 US4), `#41` (`ready-for-dev`+
  `auto/needs-human`, feat 004 US3 copy).
- **`ready-for-dev`+`auto-ok` at sweep start: 1** (`#40` only) — below the ≥3 target. See
  §4.
- **Stale/blocked:** none stale by age (board <24h old on every non-tracking issue). Not
  structurally stuck either: `#40`/`#41` are unconsumed only because this sweep runs before
  today's `/autodev`, not because nothing can pick them up — `/autodev` exists as of
  yesterday (`.claude/commands/tools/02_development/autodev.md` present) and already
  shipped `#42`/`#43`/`#44` same-day yesterday.
- **Days since last merged PR:** <1 (last merge ~15h ago, 2026-09-21 18:14 UTC). **No
  stall** (spec's threshold is ≥3 days) — stall-response section not triggered this sweep.

**Board health: Good.**

## 3. Owner-gated blocker digest

Updated `auto/owner-digest` issue `#38` in place (content changed — new sweep section
appended, one resolved blocker class removed from the standing list). See §6 in the digest
issue itself for the full current backlog by class. Two classes remain open
(browser-UI/infra: Vercel deployment protection on `#39`; owner-judgment: `#41` copy
sign-off), one new migration-class item added (`#47`, needs a human nod on RLS shape before
merge, not before build/PR).

## 4. Manufactured connector-independent work supply

Local scans run this sweep, all clean or already covered by an open issue — no security
scanner findings needed a fresh ticket:

- `npm audit --production` — **0 vulnerabilities** (yesterday's `#35`/`#42` fix holds).
- `npm run lint` — 2 errors + 11 warnings (13 problems total, well under the 004 tasks.md
  verification gate of "must not rise above 29"). Both errors are `react-hooks/refs` at
  `PoseOverlay.tsx:92` — same file/line already in `#40`'s T049 scope, so no new issue;
  instead left a clarifying comment on `#40` correcting T049's description (it was written
  against a `set-state-in-effect` error that no longer exists there; `PoseDetailContent.tsx:
  74`, the other half of T049, already lints clean — looks resolved by an earlier PR). The
  11 warnings (`@typescript-eslint/no-unused-vars`) are new findings, filed as two
  `auto-ok` issues split by codebase area (Datadog tooling vs. product/test code, per the
  spec's "scoped to one Codebase Area" rule): `#45`, `#46`.
- Targeted `grep` for risky patterns (`Math.random(`, `dangerouslySetInnerHTML`, raw
  `.innerHTML =`, `redirect(`, `eval(`/`new Function(`, credential-shaped literals) — no
  new findings. The one `dangerouslySetInnerHTML` hit (`src/app/layout.tsx:65`) is the
  known, already-reviewed `PRE_PAINT_SCRIPT` constant. Every `redirect(...)`/
  `NextResponse.redirect(...)` call either takes a hardcoded path or passes through
  `safeNextPath()` (`src/lib/auth/redirect.ts`) before use — no open-redirect surface.
- `npx tsc --noEmit` — clean. `npm test` — 531/531 passing.
- `npm run validate:poses` — 67/67 valid, Tier-1 complete; Tier-2 backfill gaps unchanged
  from prior sweeps (mostly `tradition_names` and the deeper anatomical fields on ~30
  poses). Not filed as an auto-ok issue this sweep — backfilling pose content is domain
  authorship (Sanskrit/tradition names, anatomy), not a mechanical fix, and a wrong value
  is a product-data-quality risk an unattended loop shouldn't originate without a review
  step. Left as a standing backlog note, not a ticket.
- **Dependabot:** 0 open PRs — nothing to batch-merge.

**Feature work — priority engine.** `004-sequencing-composer`'s only remaining phase
(Phase 6, US4) is already fully covered by the existing `#40`; nothing new to scope there.
Checked `003-pose-library/tasks.md` (still dependency-earlier than `004`, code long since
diverged from its own checklist — see the discrepancy note in §5): Phase 6 (US4) and
Phase 7 (US5) remain explicitly `*deferred*`, blocked on the owner sign-off gates already
tracked (`theme-taxonomy.md`, `score-explanation.md` contracts, same two `003` copy pieces
CLAUDE.md already flags as awaiting sign-off). Phase 8 (US6a — `pose_favourites`/
`pose_notes` schema, RLS, CI assertions, no UI) has no such gate ("nothing blocks Phase 8
except scheduling") and is the correct next increment to scope. Filed as `#47`, held
`auto/needs-human` — new tables are additive, but they carry RLS policies, and CLAUDE.md's
tiered policy holds RLS/policy changes to `auto/needs-human` regardless of how mechanical
the pattern is (copied near-verbatim from the existing `claimed_flows` migration). Build/PR
is fine unattended; only the merge needs a human nod on the RLS shape.

**Result: 3 `ready-for-dev`+`auto-ok` issues open** (`#40`, `#45`, `#46`) — meets the ≥3
target. Plus 2 `ready-for-dev`+`auto/needs-human` (`#41`, `#47`).

## 5. Discrepancy note (not actioned, flagged for awareness)

`specs/002-auth-tenancy-billing/tasks.md` shows 0/58 tasks checked `[X]`, but the auth/org/
billing code the tasks describe clearly exists on `main` today (`src/app/org/new/page.tsx`,
`src/app/org/[orgId]/members/page.tsx`, `src/proxy.ts`, `src/lib/supabase/*`, etc., all
present and exercised by passing tests). This reads as a stale checklist (never marked up
as work landed), not 58 outstanding tasks — no issues were manufactured from it this sweep.
Worth a human pass to either backfill the checkboxes or retire the file, so a future sweep
doesn't mistake it for real backlog.

## 6. Dev-ready issues generated/updated this sweep

- **Created:** `#45` (ESLint unused-vars, Datadog tooling, `auto-ok`), `#46` (ESLint
  unused-vars, product/test code, `auto-ok`), `#47` (feat 003 US6a schema/RLS,
  `auto/needs-human`).
- **Updated:** `#40` (comment correcting T049's current lint-error description), `#38`
  (owner-digest, rebuilt sweep section).
- **Deduplicated:** searched open+closed issues by keyword (`unused-vars`, `ESLint
  warnings`) before filing `#45`/`#46` — only prior matches were the already-closed `#36`/
  `#37`, no overlap.

## Handoff

`docs/planning/autopm/autopm-handoff-2026-09-22.md` — `/autodev`'s structured input for
today.
