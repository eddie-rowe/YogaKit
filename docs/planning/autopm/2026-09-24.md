# /autopm — Project Snapshot — 2026-09-24

**Sweep time:** ~09:30 UTC (hand-launched, scheduled slot — no scheduler exists yet,
`007-autonomous-operations` still not fully planned for the always-on runner)

## 1. Inputs read

- `docs/observation/autoobs/2026-09-24.md` (08:22 UTC, **HEALTHY**)
- `DECISIONS.md` (tail, through the 2026-09-23 read-view-SLO-false-negative entry — no
  new entries since)
- `docs/planning/ceo-brief/2026-09-23.md` (yesterday's brief)
- `docs/planning/retro/2026-09-23.md` / `docs/planning/retro/2026-09-23-reflection-40.md`
  (yesterday's retro + the delegated build agent's #40 reflection)
- `docs/planning/autopm/autodev-handoff-2026-09-23.md` (yesterday's `/autodev` handoff —
  #40 shipped as PR #66)
- Open GitHub issues + labels (the board itself)

## 2. Production health (from today's `/autoobs`)

**HEALTHY.** The `read-view-availability` SLO shows 15.096% (breached) and both burn
monitors are in Alert — this is **not a new incident**. The SLO query was corrected
2026-09-23 (per `DECISIONS.md`) to stop being blind to body-assertion failures, and it is
now honestly reporting the already-fixed 2026-09-09–09-22 outage inside its 30-day
lookback. Self-clears ~2026-10-22. Actual current state: `read-view-200` synthetic passed
574/574 runs in the last 24h, API traffic (`trace.web.request*`) healthy — 1,746
requests/24h, 0% errors, p95 ≈248ms. **Do not re-open `#39`.**

One refinement to an open finding: `#64` (three APM-derived monitors reading No Data)
now has a narrower confirmed cause — see §4.

## 3. Board health — direct from GitHub (no kanban.md/state.md)

**Good.** 10 issues closed in the last 48h (#40, #45, #46, #39, #51, #48, #50, #49, #53,
#52) via PRs #54, #55, #57, #58, #59, #63, #66 plus manual closures. 0 open PRs at sweep
start, 0 open Dependabot PRs. Last merged PR: **#66, 2026-09-23T19:18:13Z, ~14h before
this sweep** — well under the ≥3-day stall threshold, **no stall**.

No stale or blocked issues found — every open issue is ≤3 days old and already correctly
triaged (owner-gated classes below, or the standing digest).

**Open issues (8 total, before this sweep's additions):** `#38` (owner-digest, standing),
`#41` (ready-for-dev, `auto/needs-human` — copy sign-off), `#47` (ready-for-dev,
`auto/needs-human` — RLS sign-off), `#60` (`auto/needs-human` — RLS-assertion backlog,
not yet buildable), `#61` (`auto/needs-human` — US3/US4 scoping call), `#64`
(`auto/observation`+`auto/needs-human` — monitor root-cause), `#65` (`auto/needs-human` —
branch protection, repo-admin only).

**Auto-ok lane at sweep start: 0 open.** Every issue from the prior three days' backlog
(#35, #36, #37, #40, #45, #46, #50, #52, #53) has shipped or closed. This is a genuinely
empty lane, not an under-scan — see §4 for the full manufacturing pass that ran against
it.

## 4. Manufacturing connector-independent work (Step 4)

Ran the full connector-independent scan hard, specifically because the auto-ok lane
opened at zero:

- **`npm audit` (both `--production` and full, incl. devDependencies):** 0 vulnerabilities
  in either scope. Clean.
- **`npm run lint`:** 0 errors, 0 warnings, exit 0. Clean (this is the same gate #62/#63
  repaired and zeroed out on 2026-09-23).
- **`npx tsc --noEmit`:** clean, no output.
- **Targeted `grep`** for `Math.random(`, `innerHTML`, `dangerouslySetInnerHTML`,
  unvalidated `redirect(`, `path.join` on request-derived input, scoped to `src/`: the one
  `dangerouslySetInnerHTML` (`src/app/layout.tsx:65`, the pre-paint theme script) is a
  static constant, not user input — not a finding, unchanged from prior sweeps. The
  `redirect()`/`NextResponse.redirect()` calls carrying a `next` query param
  (`src/app/auth/confirm/route.ts`, `src/app/auth/callback/route.ts`) already route
  through `safeNextPath()` — no open-redirect finding. `path.join()` calls are all
  `process.cwd()` + a hardcoded subdirectory, not request input. No `TODO`/`FIXME`/`XXX`
  markers in `src/`. Nothing new.
- **Dependabot backlog:** 0 open Dependabot PRs. Nothing to flag for merge.
- **CI-flake pattern, new finding:** `db-types-check`'s `ghcr.io toomanyrequests` failure
  (first surfaced, unfixed, in yesterday's `#40`/PR #66 handoff — hit twice on that PR's
  head) hadn't been turned into a tracked issue. Filed as **`#67`**
  (`ready-for-dev`+`auto-ok`+`chore`) — a single-file CI-only retry wrapper around
  `supabase start` in `.github/workflows/ci.yml`, no schema/RLS/auth/billing touch.
- **Feature ladder (`002`→`003`→`004`→`005`→`006`):** checked every unblocked slice.
  - `003` Phase 6 (US4, T048-T056) and Phase 7 (US5, T057-T065) are both explicitly
    `*deferred*` in `specs/003-pose-library/tasks.md`, blocked on the two standing copy
    contract sign-offs (`score-explanation.md`, `theme-taxonomy.md`) — same gate as `#41`.
    Not buildable without the owner's nod regardless of mechanical simplicity.
  - `004`'s only remaining task is `T037` (owner sign-off copy) — already `#41`.
  - `002` Phase 4 (`T036`/`T040`, invitation-creation server logic — raw-token generation
    + `sha256` storage) is an auth-security-sensitive change — held, matches the "all auth
    changes stay `auto/needs-human`" policy exactly, not filed as a new issue since it's
    not auto-ok material.
  - `002` Phase 5/6/7 (RLS assertions, cohort UI, Stripe billing) already fully covered by
    `#60`/`#61` — re-verified accurate, not re-filed.
  - `002` Phase 8 (polish/validation, T061-T066) requires "all five user stories
    independently functional" per its own checkpoint — not reachable yet (US2/US3/US4
    still have open tasks). Correctly not buildable.
  - `005-daily-sadhana` has no `tasks.md` yet (spec + design-input only) — planning it is
    its own `/speckit-plan`+`/speckit-tasks` pass, out of scope for issue-mining this
    sweep.
- **Stale local branches** (`002-org-invitations`, `claude/tender-pasteur-sq1nea`,
  `feat/auth-clickthrough`, `feat/rum-full-observability`,
  `fix/rum-approuter-not-mounted`, `fix/rum-view-churn`, and several already-merged
  `feat/*`/`fix/*` branches from 2026-09-22/23): checked, all diverged/already-landed —
  repo hygiene, not work supply, not actioned (branch deletion is a destructive action
  outside this sweep's non-destructive mandate).

**Result: auto-ok lane 0 → 1 (`#67`).** Genuinely below the ≥3 target. This is not a
scan-effort gap — every connector-independent source (security, lint, types, dependabot,
the full feature ladder) came back clean or already correctly held. Recorded honestly
rather than manufacturing busy-work; see the owner-digest for the implication (the next
`#41`/`#47` sign-off unblocks real feature work, which is the actual lever here).

## 5. `#64` refinement (not a new issue — added as a comment, evidence update only)

Today's `/autoobs` Finding #2 re-ran `#64`'s own `search_datadog_metrics` check with a
name filter and confirmed the metric family all three affected monitors query
(`trace.next_js.BaseServer.handleRequest*`) **does not exist at all** for this service —
narrowing `#64` from three separate hypotheses to one shared, confirmed one. Concrete fix
is now clearer (retarget at `trace.web.request*`) but still held `auto/needs-human`: `#64`
asks for independent root-causing of all three monitors plus a `validateLiveTelemetry`
tag-set fix, which is broader than the one-line retarget and stays a human call per the
issue's own multi-part ask. Comment added with the narrowed evidence, issue not
re-labeled.

## 6. Owner-gated backlog, by class (current)

1. **owner-judgment** (`#41`, age ~64h → ~88h) — sharing/revoke copy (FR-032) sign-off.
2. **migration** (`#47`, age ~24h → ~48h) — `pose_favourites`/`pose_notes` RLS shape
   sign-off.
3. **roadmap/scope** (`#60` RLS-assertion backlog, `#61` US3/US4 scoping) — unchanged,
   re-verified accurate.
4. **observation/investigation** (`#64`, narrowed today) — monitor root-cause, needs a
   human decision on re-query vs. retire per monitor.
5. **repo-admin** (`#65`) — branch protection / required checks, needs `main`
   admin access, cannot be set by automation.

No new owner-gated items this sweep.

Handoff to `/autodev`: `docs/planning/autopm/autopm-handoff-2026-09-24.md`.
