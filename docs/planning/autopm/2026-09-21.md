# /autopm — 2026-09-21

**Sweep time:** run at scheduler fire, 2026-09-21 (UTC date; exact time not logged by
this ad hoc run — no `/autopm` command file exists yet to standardize the header).

**Overall:** DEGRADED (infra gaps below; no dev-blocking failures found in the work
itself).

## Infra note (read first)

`.claude/commands/tools/01_project_management/autopm.md` — the file this run's
schedule named as "authoritative spec, hardened for this unattended run" — does not
exist anywhere in this repository (no command file, no skill registration, no prior
entry in `docs/planning/routine-log.md`). Likewise absent: `kanban.md`, `state.md`,
`docs/observation/connector-reports/*.json`, any CEO-brief or retro-note file, and any
prior GitHub issue (0 issues of any state existed before this sweep, so the schedule's
references to issues #76/#77 do not resolve — nothing by those numbers exists in this
repo).

This looks like the first time `/autopm` has been asked to run against this repo
specifically (the parallel `/autoobs` routine *is* ported and has two prior runs:
2026-09-04, 2026-09-21). Rather than abort, this run followed the tiered-auto-ok /
single-owner-digest / manufacture-connector-independent-work policy exactly as spelled
out inline in the scheduled prompt itself, and recorded the gap here and on the
owner-digest issue instead of fabricating history that doesn't exist.

## Inputs actually available this sweep

- `docs/observation/autoobs/2026-09-21.md` — today's `/autoobs` digest (exists, fresh,
  02:21 UTC). No JSON sidecar exists at any date, so none was used.
- `DECISIONS.md` — read in full; no ratified Increment-1 roadmap decision found, so no
  feature issues were scoped this sweep (per the standing "only after ratification"
  rule).
- `FRICTION.md` — read; no items rise to a dev-ready issue on their own this sweep.
- No `kanban.md`, `state.md`, CEO brief, or retro note exist to read.

## Manufactured connector-independent work

Ran `npm audit --json` and `npm run lint` (both fully local, no connector dependency)
plus a targeted `grep` pass for common injection/secret patterns
(`dangerouslySetInnerHTML`, `eval(`/`new Function(`, inline credential-shaped
literals).

- **`npm audit`**: 13 vulnerabilities — 1 critical (`next`, unauthenticated RCE via
  Image Optimization + AVIF, `GHSA-2xp9-vwfh-vxw4`, plus a Windows-RCE advisory in the
  same range), 7 high, 5 moderate. Every one has a fix available with no semver-major
  bump required (`next` 16.2.9 → 16.3.5 clears the critical/high `next`/`postcss`/
  `sharp` entries; `npm audit fix` clears the rest). Filed as
  eddie-rowe/YogaKit#35, auto-ok (dependency bump, no schema/RLS/auth/payments).
- **`npm run lint`**: 22 errors / 11 warnings. Two clusters filed:
  - eddie-rowe/YogaKit#36 — `react/no-unescaped-entities` (4 files) +
    `no-html-link-for-pages` (1 file): one-line mechanical fixes, no behavior change.
  - eddie-rowe/YogaKit#37 — `react-hooks/set-state-in-effect` (9 files, all the same
    "hydrate state from localStorage/searchParams on mount" shape): mechanical but
    touches more surface, called out separately with a RULE-L3/L4 offline-read
    regression-check note since several of the 9 files are the flow/sequence
    localStorage-cache pages.
  - The remaining 11 `no-unused-vars` warnings were left unfiled — pure warnings, no
    CI gate depends on them, not worth a fourth issue this sweep; can be swept into
    whichever of #36/#37 touches those files, or picked up standalone later.
- **grep security pass**: one `dangerouslySetInnerHTML` (`src/app/layout.tsx:65`,
  a fixed `PRE_PAINT_SCRIPT` constant for theme-flash prevention, not user input — not
  a finding). No `eval`/`new Function`, no hardcoded-secret-shaped literals. Nothing
  filed.

Total: 3 `ready-for-dev` + `auto-ok` issues open and handed to `/autodev`
(eddie-rowe/YogaKit#35, #36, #37) — meets the "≥3 auto-ok" target for this sweep.

## Owner digest

Created eddie-rowe/YogaKit#38 (`owner-digest` label) as the single, idempotent status
issue — no prior one existed. Carries: the infra-gap note above, today's `/autoobs`
AT-RISK finding (read-view synthetic failing, needs a human to check Vercel
deployment-protection settings — not a code fix), and the #35/#36/#37 summary. Future
sweeps should update this issue in place rather than opening a new one.

## Not run / out of scope this sweep

- No Increment-1 feature-issue scoping (roadmap not yet ratified in `DECISIONS.md`).
- No attempt to fix the Vercel deployment-protection issue directly — outside this
  routine's scope (operator action, not a code change).
- No Dependabot PRs existed to flag for merge (0 open PRs in the repo at sweep start).

## Re-run 17:06 UTC — `.claude/commands/tools/01_project_management/autopm.md` now exists

Third `/autopm` fire today, and the first against the real hardened spec (landed in
`28331ec`, "retarget NextMove-ported command tooling for YogaKit") rather than the inline
scheduled-prompt fallback the first two runs used. Ran the full flow the spec describes:
project snapshot (this file), board health via GitHub directly, owner-digest rebuild,
connector-independent work manufacture, dev-ready issue generation, handoff file, and
`routine-log.md` line.

**Overall: DEGRADED, unchanged in kind** — same production AT-RISK condition
(`docs/observation/autoobs/2026-09-21.md`, read-view synthetic failing 100% across 3
redeploys today), no new dev-blocking failure in the work itself.

### Board health (direct GitHub query, not a `kanban.md`/`state.md` file)

- Closed in last 48h: **0**.
- Open issues: **6** — `ready-for-dev`+`auto-ok`: #35, #36, #37, #40 (4). `ready-for-dev`
  held `auto/needs-human`: #41 (1). `auto/observation`: #39 (1, opened by `/autoobs` at
  16:59). Plus the owner-digest itself, #38.
- Open PRs: **0**. No Dependabot backlog to flag.
- **Stall signal (spec §Error Handling): ≥3 days since last merged PR.** Last merged PR
  is `#34` (2026-09-10) — **11 days**, well past the 3-day threshold. Per the stall
  response: verified Step 4 (manufacture) was not the gap — the auto-ok lane already had
  3 open issues going into this sweep, so this run generated real feature work instead of
  only re-ranking the owner-digest (see below).
- Board health verdict: **Needs Attention** — not Critical (no active incident from the
  planning side; the production AT-RISK item is tracked separately via #39/the digest),
  but structurally stuck: every `ready-for-dev` issue has no consumer because `/autodev`
  doesn't exist yet (`007-autonomous-operations` still Draft).
- Nothing stale/blocked by age (board is entirely <15h old); no destructive board actions
  taken or needed.

### Manufactured / generated this re-run

- Re-ran `npm audit` (both `--production` and full) and `npm run lint` — byte-identical
  to the morning sweep (13 vulns: 1 critical/7 high/5 moderate; 22 lint errors/11
  warnings). #35/#36/#37 still accurately describe `main`; nothing new to file from the
  scanners.
- **Corrected the earlier "no ratified Increment-1 roadmap decision" gate**: that
  condition was this loop's own invented fallback from its first, spec-less run — the
  real `autopm.md` asks for `feat:` issues cross-referenced against each feature's
  `tasks.md` directly, with no roadmap-ratification precondition. `specs/004-sequencing-
  composer/tasks.md` is 41/49 done; the 8 open tasks are Phase 6 (US4, T043-T049, all
  auto-ok) and T037 (US3, owner-sign-off copy). Filed:
  - **eddie-rowe/YogaKit#40** — `feat(004 US4)`: decompose `ComposeClient.tsx` into
    `src/components/compose/` (T043-T049) — component refactor + 2 `set-state-in-effect`
    fixes distinct from #37's files. `ready-for-dev`+`auto-ok`.
  - **eddie-rowe/YogaKit#41** — `feat(004 US3)`: draft T037 sharing/revoke copy (FR-032)
    for owner sign-off — reframed per this run's operating context ("reframe owner-gated
    blockers into buildable work where possible") into a draft-now/approve-before-merge
    shape, mirroring the two already-staged `003` copy contracts. `ready-for-dev`, held
    `auto/needs-human` (owner-judgment: copy sign-off, not a technical risk).
  - `specs/003-pose-library/tasks.md` (47/80 done) and `specs/005-daily-sadhana`/`006-
    profile-settings` (no `tasks.md` yet, no plan) were checked but not scoped further
    this sweep — `003`'s remaining 33 are predominantly Tier-2 pose backfill (opportunistic
    per the constitution, not a forcing function), and `005`/`006` have no task breakdown
    to cross-reference yet (a planning gap, not a coding one).
- **Total auto-ok issues open: 4** (#35, #36, #37, #40) — above the ≥3 target.
- **Total `ready-for-dev` issues open: 5** (adds #41).

### Owner digest

Rebuilt eddie-rowe/YogaKit#38's **body** in place (not a new comment — a stray comment
was posted in error mid-sweep, then edited down to a one-line redirect once the mistake
was caught, so the body stays the single source of truth). Added: the stall finding, the
two new issues, #39, and a three-item owner-gated blocker list (roadmap-gate,
browser-UI/infra, owner-judgment for #41's copy sign-off).

### Not run / out of scope this re-run

- No attempt to fix the Vercel deployment-protection issue directly (operator action).
- `005`/`006` planning (spec→plan→tasks) not run — out of this routine's scope; it
  generates issues from existing `tasks.md` breakdowns, it doesn't author them.
