# CEO Brief — 2026-09-26

**TL;DR:** Nothing merged today, but the loop built and proved everything it could reach
unattended — two more PRs, both gate/CI-green, one of them catching a real touch-target
bug along the way. The entire backlog is now waiting on you, not on the loop: 8 open PRs
across sign-offs, scoping, and one blocked tooling permission. Clearing any of them
keeps the next dev sweep fed.

## State of the business

**Built today, held for your review (not merged):**
- `#98` — drafted `005-daily-sadhana`'s `plan.md`/`tasks.md`, resolving 11 of 13 open UX
  decisions against their own recommended defaults. 2 need your nod before the plan is
  final (no streak-repair mechanic; guidance-card copy tone). Gate and CI green.
- `#99` — built `003`'s filter affordances and score explanations (complexity/injury-risk
  badges, in-place score explanations, checkbox affordance on filter chips). Along the
  way it found and fixed a real bug: selecting certain filter chips was silently
  dropping the 40px minimum touch-target size. Gate and CI green; browser validation
  honestly deferred — see below.

**Blocked, not broken:** `#96` (syncing one Datadog synthetic's threshold, a single
already-decided command) couldn't run — this session's own safety controls block live
writes to third-party infrastructure even though the task itself is pre-cleared. Needs
either a human to run one command, or a decision to route this class of action through a
different, more-permissioned execution path.

**Production:** still HEALTHY. Error rate 0%, latency ~43ms, both core synthetics
passing effectively 100% of the time. The "breached" SLO number on the dashboard is
still the already-fixed mid-September outage aging out of its 30-day window — self-
clears around October 22, no action needed.

**New, worth a decision:** the preview links this loop uses to browser-test UI changes
before merge now require signing into Vercel first — so as of today, no public-facing UI
change can get real pre-merge browser evidence at all. This has happened for two
different reasons in two consecutive days; it's becoming a standing gap rather than a
one-off.

## Next 3 moves

1. Clear one of today's two new PRs (`#98` or `#99`) to keep tomorrow's build queue fed
   — both are ready to merge pending your nod on the flagged decisions.
2. Decide how the loop should handle live-infrastructure commands like `#96`'s — a
   one-line policy call (run it yourself when it comes up, or grant a specific higher-
   permission path) closes a gap that's now recurred twice.
3. Decide on the Vercel preview-access gap: either provision a way for the automation to
   see previews pre-login, or accept that browser evidence on public UI PRs waits for a
   human until that's fixed.

## What I need from you

Seven open asks, none urgent, none blocking production — full detail on issue `#38`:

1. `#47` — RLS shape sign-off on `pose_favourites`/`pose_notes`. Built (`#90`), but that
   PR also needs its generated types refreshed before it's even mergeable.
2. `#60` — RLS test-coverage sign-off. Built (`#91`), CI green, just needs your nod.
3. `#61` — entitlements/cohort/billing sign-off. Built across 4 stacked PRs (`#92`–
   `#95`), all CI green, just needs your nod.
4. `#82` — filter/score-explanation copy sign-off. Built (`#99`, today), CI green.
5. `#83` — theme-taxonomy copy sign-off. Not yet built — the patch is large enough to
   warrant re-scoping before the loop schedules it.
6. `#96` — one Datadog sync command someone with the right access needs to run.
7. `#97`/`#98` — two of 13 sadhana-plan decisions need your nod (streak-repair,
   guidance tone).

## Drift/coherence flags

None new in the codebase. The Vercel preview-access gap above is a process flag, not a
code coherence issue.

Standing punch list: issue `#38`.
