# Requirements Quality Checklist: Krama MVP

**Purpose**: "Unit tests for requirements" — validate that the spec is complete,
unambiguous, consistent, and measurable before planning begins.
**Created**: 2026-06-22
**Updated**: 2026-06-22 (post clarify/plan/analyze passes)
**Feature**: [spec.md](../spec.md)

---

## Safety Requirements Quality

- [x] CHK101 Contraindication vocabulary enumerated in data-model.md (30 canonical slugs).
  [Clarity, Spec §FR-003, §Hard Constraints — RESOLVED]

- [x] CHK102 FR-015a specifies safety layer attempts replacement in same body-position family;
  gap inserted with safetyNote if no replacement exists. [Clarity, Spec §FR-015a — RESOLVED]

- [x] CHK103 FR-015 updated with measurable definition: "no poses with difficulty:'advanced'
  for beginner sessions; no poses designated intermediate for beginner; etc."
  [Ambiguity, Spec §FR-015 — RESOLVED]

- [x] CHK104 FR-015a: gap with rebound/rest pose inserted when no valid replacement; teacher
  informed with plain-language safetyNote. [Edge Case — RESOLVED]

- [x] CHK105 A-013 notes contraindication vocabulary is extensible via PR; schema validation
  enforces new additions. [Completeness — RESOLVED]

- [x] CHK106 research.md and plan.md explicitly state safety layer is "pure, deterministic
  TypeScript module with no external I/O." [Clarity — RESOLVED]

- [x] CHK107 FR-009 requires `prop_free_variation` field on every pose (nullable); FR-015a
  checks this field before replacement. Not every pose needs one — null is valid when the
  pose fundamentally requires props. [Completeness — RESOLVED]

---

## Hybrid Pipeline Requirements Quality

- [x] CHK108 data-model.md defines `PipelineDraft` as the typed interface between AI layer
  and rules engine. [Completeness — RESOLVED]

- [x] CHK109 FR-006c added: "AI layer MUST be treated as untrusted input by the rules engine
  and safety layer." Now a functional requirement. [Traceability — RESOLVED]

- [x] CHK110 research.md explicitly separates domains: rules engine handles sequencing/
  bilateral/transitions; safety layer handles contraindications/props/intensity ceiling.
  [Clarity — RESOLVED]

- [x] CHK111 research.md: fallback produces a `ConstrainedSequence` with
  `generationSkipped:true`; safety still runs; thematic "why" generated by T052 fallback
  logic. [Completeness — RESOLVED]

- [x] CHK112 FR-012 applies always (not AI-only); T052 generates dimensional "why" in
  rules engine when AI draft is absent or generic. [Consistency — RESOLVED]

---

## Data Model & Pose Library Requirements Quality

- [ ] CHK113 FR-010 specifies 40+ pose count. Meridian coverage (all 12 meridians
  represented) is implied by the seed list in T014–T018 but not stated as an explicit
  requirement. [Measurability — OPEN (LOW priority; does not block P1)]

- [x] CHK114 FR-011 + data-model.md defines `PoseMode` with `tissue_target`
  (connective/muscular/both) and `hold_range` in minutes. The yin/yang distinction is
  operationally defined by mode type + tissue target. [Clarity — RESOLVED]

- [x] CHK115 contracts/pose-library-schema.md specifies slug regex `^[a-z][a-z0-9-]*$`,
  uniqueness constraint, and file-name-must-match-slug rule. [Clarity — RESOLVED]

- [x] CHK116 data-model.md defines `ElementRecord` and `MeridianRecord` with element,
  season, organ, direction, themes, emotions, and body_focus fields. [Completeness — RESOLVED]

- [x] CHK117 data-model.md: `counterposes: string[]` (pose slugs), `rebound_pose: string | null`.
  [Clarity — RESOLVED]

- [x] CHK118 data-model.md: `HoldRange { min: number; max: number }` in minutes. Yin holds
  2–8 min typical; yang as seconds. [Completeness — RESOLVED]

- [x] CHK119 `EnergeticQ` fully enumerated: grounding|opening|cooling|heating|calming|
  stimulating. Used as enum in pose records and AI prompt filtering. [Clarity — RESOLVED]

---

## Dimension Input Requirements Quality

- [x] CHK120 A-013 enumerates all dimension allowed values for P1 (Style, Season, Dosha,
  FiveElement, BodyPosition, EnergeticQ, IntensityCurve, ExperienceLevel, PoseDifficulty).
  [Completeness — RESOLVED]

- [x] CHK121 T056 specifies season↔element auto-suggestion in MeridianElementPicker. The
  mapping is derivable from meridian data files. [Clarity — RESOLVED]

- [x] CHK122 A-013 defines `IntensityCurve` enum values; T057 creates the picker with visual
  sparkline preview. [Completeness — RESOLVED]

- [x] CHK123 FR-002 + T059 `resolveDefaults()` fills all unset dimensions with sensible
  values before generation. Defaults: 60min, yin style, mixed level, all props available.
  [Clarity — RESOLVED]

---

## Privacy & Security Requirements Quality

- [x] CHK124 FR-006b gives concrete example: "e.g., 'hypertension,' 'second-trimester
  pregnancy'" as the anonymized form. Developers know exactly what to exclude.
  [Clarity — RESOLVED]

- [ ] CHK125 No UI-level prevention or warning if a teacher accidentally types PII (e.g.,
  student name) into a constraint field. T027a tests backend anonymization but no frontend
  safeguard exists. [Edge Case — OPEN (MEDIUM priority; consider a P2 UX improvement)]

- [ ] CHK126 No data retention/deletion flow specified. Teacher cannot clear all local data
  from the app. [Completeness — OPEN (MEDIUM priority; does not block P1)]

- [x] CHK127 research.md and quickstart.md document ANTHROPIC_API_KEY storage in Vercel
  environment variables. [Completeness — RESOLVED]

---

## Performance Requirements Quality

- [x] CHK128 SC-010 says "95% of requests" — this IS a P95 target. Explicit.
  [Clarity — RESOLVED]

- [ ] CHK129 30-second target not qualified by network conditions (WiFi vs. mobile).
  [Clarity — OPEN (LOW priority; post-launch monitoring concern)]

- [ ] CHK130 5-second rules-engine fallback target not specified for what pose library size.
  At 40 poses it is trivially achievable; at 5,000 poses it may not be.
  [Completeness — OPEN (LOW priority; not a P1 concern)]

- [x] CHK131 RULE-L6 (Lighthouse ≥ 90) is in plan.md constitution check table and T073
  is the task. [Traceability — RESOLVED]

---

## Scenario & Acceptance Criteria Quality

- [x] CHK132 Edge case in spec covers AI unavailability: "falls back to rules-engine-only
  silently and presents the result without surfacing an error."
  [Coverage — RESOLVED]

- [ ] CHK133 First-time/empty-state scenario not explicitly defined (what teacher sees before
  any generation). [Coverage — OPEN (LOW priority; UI design decision, not a spec gap)]

- [ ] CHK134 Font size quantification stated in T067 (32px for pose name, 20px for cue)
  but NOT in spec. SC-007 says "legible" without a number.
  [Measurability — OPEN (MEDIUM priority; add to SC-007 or US6)]

- [x] CHK135 SC-003 is measurable: "Zero generic anatomical-only explanations — every 'why'
  references at least one dimension." [Measurability — RESOLVED]

- [x] CHK136 US2 has an independent test criterion that does not require US1 to be complete.
  [Coverage — RESOLVED]

---

## Completeness & Consistency

- [x] CHK137 A-012 explicitly defers breath/meditation bookends to P2.
  [Completeness — RESOLVED]

- [x] CHK138 A-012 explicitly defers soundscape suggestion to P2.
  [Completeness — RESOLVED]

- [x] CHK139 A-012 explicitly defers student handout output to P2.
  [Completeness — RESOLVED]

- [x] CHK140 A-012 explicitly defers visualizations (sankey, intensity curve) to P3.
  [Completeness — RESOLVED]

- [x] CHK141 T070 adds bilateral side indicator to the timer/deliver view.
  [Completeness — RESOLVED]

- [x] CHK142 FR-021: "stored locally on their device" + IndexedDB persists across app
  close/reopen. [Clarity — RESOLVED]

---

## Summary

| Status | Count |
|--------|-------|
| Resolved [x] | 35 |
| Open [ ] — MEDIUM or LOW priority | 7 |
| Open [ ] — CRITICAL or HIGH | 0 |

**Open items do not block P1 implementation.** CHK113, CHK125, CHK126, CHK129, CHK130,
CHK133, CHK134 are LOW–MEDIUM priority improvements for P2 or post-launch.
